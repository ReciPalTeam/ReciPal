REPLIT PROMPT — ReciPal “My Recipes” UI (Cards + Detail) — Bulletproof, No Freelancing

You are working in the ReciPal codebase. The Instacart shopping list integration is DONE and WORKING. Do NOT modify anything related to Instacart endpoints, payloads, env vars, URL resolution, or unit normalization. This task is UI + wiring only for “My Recipes”.

GOAL
Make the “My Recipes” items that are currently being logged/stored appear in the “My Recipes” section as:
1) Formatted meal cards that match the For You feed recipe cards EXACTLY (same layout, typography, spacing, button(s), hover states, skeleton/loading, etc.)
2) Each card must include the ReciPal logo in the exact same place on the card for all My Recipes items (use existing logo asset / component; do not invent a new logo)
3) Clicking a My Recipes card opens the FULL expanded recipe view that is identical to the expanded card / detail experience used by For You feed recipes (same layout and same buttons)
4) The card must show ingredients (same format as For You feed cards do). If the feed card shows only a preview count, match that behavior. If it lists top N ingredients, match that exactly.
5) The primary action button(s) must behave the same as For You feed (same label, same click handler behavior). If For You uses “Cook”, “View”, “Add to cart”, “Add ingredients”, etc., My Recipes must use the same component and call the same handler(s) with the correct recipe id.

ABSOLUTE RULES (NO FREELANCING)
- Reuse existing components from the For You feed. Do not rebuild a “similar” card.
- Do not change existing UI styles for the feed. The goal is parity.
- Do not create a second recipe detail page/layout. Reuse the existing expanded/detail component used by the For You feed.
- Do not add new libraries.
- Do not refactor unrelated parts of the app.
- Do not change data model fields unless strictly required to render the UI.
- If you need to add a tiny adapter mapping “My Recipe” shape → “Feed Recipe” shape, do that in one place and keep it minimal.

WHAT “MY RECIPES” MEANS
There is already a place where “My Recipes” are being logged/stored (DB table, file store, local storage, server route, etc.). Your first step is to LOCATE THE SOURCE OF TRUTH by searching the repo for:
- “My Recipes”
- “my_recipes”
- “saved recipes”
- “bookmark”
- “recipe_saved”
- “recipe_log”
- any events/telemetry indicating recipe save/logging

Then:
- Identify the existing read path (or create the smallest possible read path) that returns an array of “My Recipe” items to the client.

REQUIRED UX / UI SPEC
A) My Recipes List
- Add / update the “My Recipes” section/page so it renders a grid/list of cards.
- The card must be the exact same component used in For You feed. Prefer importing and reusing that component directly.
- Ensure sorting: newest-first (by savedAt/createdAt timestamp). If timestamp doesn’t exist, preserve the existing order from storage.
- Empty state: if no My Recipes exist, show the same style empty state pattern used elsewhere (do not create a new design language).
- Loading state: if For You uses skeleton cards, use the same skeleton component.

B) My Recipes Card Content
- Must display: title, image (if available), and ingredient preview in the exact same format as For You feed cards.
- Include ReciPal logo on the card (use existing logo component/asset). Place it consistently across My Recipes cards. If feed cards already show branding, match that pattern; otherwise add logo in the least invasive way that does not break layout.

C) My Recipes Detail / Expanded View
- Clicking a My Recipes card must open the same expanded recipe detail experience used by For You feed.
- The route/interaction must match For You behavior (modal vs page route). Do not invent a new pattern.
- Buttons in the detail view must function exactly like For You detail view, including “Add ingredients to cart” flow (but do not touch Instacart code; just call the existing client actions).

DATA / SHAPE REQUIREMENTS
- If My Recipes items are stored with a different shape than the feed recipe object, create a minimal mapping function:
  mapMyRecipeToFeedRecipe(myRecipe) -> feedRecipeShape
- Only map the fields required by the existing card/detail components. Do not expand scope.
- If My Recipes are missing fields required by the feed components (e.g., imageUrl, ingredients array), fill with safe fallbacks that mimic existing app behavior (e.g., placeholder image, empty ingredients list, etc.).
- Do not degrade For You feed behavior.

IMPLEMENTATION PLAN (DO THIS IN ORDER)
1) Identify the For You feed recipe card component and the expanded/detail component it navigates to.
   - Document the component paths.
   - Confirm the props shape it expects.
2) Identify where My Recipes are currently logged/stored and how they can be read.
   - If there is already a GET endpoint, use it.
   - If not, add ONE minimal server route:
     GET /api/my-recipes  -> returns array of saved recipes for the current user (however user/session is handled in this app)
3) On the client:
   - Create/update the My Recipes page/section to fetch and render the list.
   - Use the EXACT same card component as For You feed.
   - Wire click-through to the existing detail flow.
4) Add ReciPal logo to the My Recipes cards using existing asset/component.
5) Add/Update tests:
   - Rendering list with N items
   - Empty state
   - Clicking a card opens detail view
   - Component parity: ensure the My Recipes uses the same card component import as For You (assert on component usage if possible)
6) Verify locally:
   - Start app, go to My Recipes, see cards, click into detail, run primary action button, ensure no regressions.

FILES: CONSTRAINTS
- Only touch files necessary for this feature:
  - My Recipes page/route
  - Minimal API route if missing
  - Minimal data adapter/mapping helper
  - Tests related to My Recipes
- Do not modify Instacart files, unit conversion pipeline, or telemetry pipeline unless required to READ My Recipes.

ACCEPTANCE CRITERIA (MUST ALL PASS)
- My Recipes shows saved items as cards that are visually identical to For You feed cards.
- Cards include ReciPal logo consistently.
- Clicking any My Recipe opens the same expanded recipe detail as the feed (not a new UI).
- Primary button(s) behave the same as For You feed (same actions, no broken calls).
- No changes to Instacart integration behavior.
- Tests pass; build passes.

OUTPUT REQUIREMENTS
When done, respond with:
1) The exact files changed (list)
2) Short summary of what was wired where
3) Any assumptions you had to make about “My Recipes” storage and how to change it if storage differs

BEGIN NOW. First step: locate the For You card + detail components and the My Recipes storage/read path.
