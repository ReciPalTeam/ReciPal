We are integrating FatSecret as the recipe + nutrition provider.
IMPORTANT CONTEXT:
- The app currently uses mockRecipes as the canonical recipe contract.
- We are NOT integrating Instacart yet.
- The immediate goal is to replace mockRecipes with API-backed recipes
  WITHOUT breaking existing UI (feed, recipe detail, planner).
- Therefore, mockRecipes’ shape is the target output contract.

Please inspect the codebase and return the following EXACT information
(no guesses, no refactors, no placeholders).

---

A) Canonical Recipe Contract (CRITICAL)
1) Confirm explicitly:
   - Is the Recipe type in client/src/lib/mock-data.ts the canonical contract
     expected by ALL current UI paths (feed, recipe detail, planner)?
   - Are any UI paths using the Recipe type from buildForYouFeed.ts directly?
2) Paste the full Recipe type from:
   - client/src/lib/mock-data.ts
   - client/src/lib/feed/buildForYouFeed.ts
3) If there are differences between these two shapes, list them explicitly
   in a bullet list (field name + type difference).

---

B) Feed Runtime Verification
1) In client/src/pages/recipes/index.tsx:
   - Identify the exact recipe object being rendered in the feed loop.
   - Paste the JSX render loop and show which recipe fields are accessed
     (image vs imageUrl, cookTime string vs number, macros location, etc).
2) State clearly:
   - “Feed uses mockRecipes” OR
   - “Feed uses buildForYouFeed() output”

---

C) Ingredient Normalization & Overlap Logic
Paste the FULL code (not summaries) for:
- normalizeIngredientName(name)
- getPantryOverlap(recipe)
- getIngredientFoodGroup(name)

Include file paths above each snippet.

---

D) FoodGroup Definition
1) Paste the FoodGroup enum/union definition.
2) Paste any lookup tables or mapping logic used to assign FoodGroup.

---

E) Ingredient Amount Handling
1) Search the codebase for:
   - parseFloat
   - Number(
   - regex parsing of ingredient amounts
2) Paste any occurrences.
3) If none exist, explicitly state:
   “Ingredient.amount is always treated as a string and never parsed.”

---

F) Planner & Macro Computation Dependency
1) Paste planner-totals.ts.
2) Identify which recipe fields are REQUIRED for macro computation.
3) Confirm whether macros are assumed per-serving or per-recipe.

---

OUTPUT FORMAT:
1) Short bullet summary of findings (A–F).
2) Then paste raw code snippets with file paths.
3) Do NOT refactor or suggest changes. Only report what exists.
