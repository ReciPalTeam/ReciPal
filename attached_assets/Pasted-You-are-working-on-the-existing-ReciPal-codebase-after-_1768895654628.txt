You are working on the existing ReciPal codebase after Prompts P1–P4 have been applied and completed.

This prompt is NOT adding new features. It is a stabilization, QA, and regression safety pass.

ABSOLUTE RULES
- Do NOT change any UI layouts, card layouts, navigation, or styling.
- Do NOT change any labels or copy except for error messages/toasts if necessary for clarity.
- Do NOT change the Recipes feed card UI at all.
- Do NOT change Pro gating rules (RevenueCat entitlement remains source of truth).
- You may only fix bugs, tighten logic, and add test coverage + lightweight debug aids.

GOAL
1) Verify and fix correctness across:
   - Planner Plan/Track toggle (blur lock for free)
   - summary bar totals
   - cooked/remove buttons and state
   - midnight auto-count logic and no double-counting
   - auto-populate preview overlay (checkboxes + servings + confirm fills empty only)
   - Add-to-Plan scheduling popup (single/range/multi-day + replace warning)
   - ingredient swaps (labels + swap popup + persistence per-entry)
   - swap nutrition recalculation impacting projected totals (preview) and counted totals (cooked/auto-counted)
   - Free non-planner logs (Cook Now + checkout completed) affecting day/week totals without visible planner entries
   - Pro manual custom entry affecting day/week totals without visible planner entry
2) Add unit tests for the most fragile logic paths.
3) Add a concise QA runbook checklist for manual testing before shipping.

────────────────────────────────────────
A) AUDIT + FIX: PLANNER TOTALS INTEGRITY
────────────────────────────────────────

A1) Prevent double counting
- Ensure a meal counted via auto-count at midnight cannot be counted again when user taps Cooked.
- Ensure removing a previously counted meal subtracts it exactly once.

A2) Midnight rollover correctness
- Confirm rollover uses local timezone date boundaries.
- Confirm lastRolloverDate logic prevents repeated rollover on the same day.

A3) Day/Week totals must include:
- Planner meals that are cooked OR autoCounted
- ConsumptionLog entries created by:
  - cooknow_logged_recipe
  - checkout_logged_recipe
  - manual_custom_entry (Pro only)
But must NOT include:
- scheduled meals that are not cooked/autoCounted yet

A4) If recipe nutrition missing:
- totals must still compute calories (fallback)
- macros may be blank/estimated, but no crashes

Fix any bugs found. Do not alter UX.

────────────────────────────────────────
B) AUDIT + FIX: SWAPS PERSISTENCE + SCOPE
────────────────────────────────────────

B1) Swap persistence per planned entry
- Verify ingredient overrides persist across app reload
- Verify swaps apply only to that specific planned meal entry and do not affect:
  - other identical meals on other days
  - the base recipe definition used elsewhere

B2) Nutrition recalc
- After a swap, ensure nutrition totals shown in:
  - recipe detail
  - planner meal detail
  - preview projected totals (where applicable)
remain consistent and update immediately.

B3) Instructions unchanged
- Verify instructions text never changes after swaps.

Fix any issues.

────────────────────────────────────────
C) AUDIT + FIX: AUTO-POPULATE PREVIEW + CONFIRM RULES
────────────────────────────────────────

C1) Preview overlay must not write to planner before confirm.
C2) Confirm plan must fill ONLY empty slots (never overwrite).
C3) Desserts/Snackitizers only generated when checkboxes enabled.
C4) Default max:
- 1 dessert/day
- 1 snackitizer/day
C5) Serving size steppers must scale projected totals correctly.

Fix bugs only.

────────────────────────────────────────
D) AUDIT + FIX: ADD-TO-PLAN SCHEDULING POPUP
────────────────────────────────────────

D1) Validate selection modes:
- single day
- date range
- multi-select days

D2) Replace warning correctness:
- must trigger only for selected days that already have a meal in that slot
- replacement must only affect those selected conflicts

D3) Entries are not linked:
- editing/swapping one day does not affect another

Fix bugs only.

────────────────────────────────────────
E) PRO GATING INTEGRITY (REVENUECAT)
────────────────────────────────────────

E1) Track mode
- Free users see blurred placeholder Track UI; no tracking computations or persistence.
- Pro users see real Track UI.

E2) Manual Add
- Only visible to Pro.
- Writes manual_custom_entry logs correctly and affects totals.

Fix any gating leaks.

────────────────────────────────────────
F) ADD TEST COVERAGE (MINIMUM VIABLE)
────────────────────────────────────────

Create pure functions for testability if needed, without changing behavior:
- computePlannerTotals(...)
- applyMidnightRollover(...)
- buildPreviewWeekPlan(...)
- applyMealReplacement(...)
- applyIngredientSwap(...)

Add unit tests (Vitest preferred if available, otherwise Jest) for:
1) Midnight rollover counts scheduled -> autoCounted exactly once.
2) Cooked after autoCount does not double-count.
3) Remove subtracts counted meals correctly.
4) Confirm Plan only fills empty slots, never overwrites.
5) Scheduling replace warning only affects selected conflicting days.
6) Swaps persist per entry and do not affect other days.

Add “How to run tests” to README.

────────────────────────────────────────
G) QA RUNBOOK (MANUAL CHECKLIST)
────────────────────────────────────────

Add /docs/QA_RUNBOOK_POST_P4.md with a short checklist:

- Plan/Track toggle behavior (free vs pro)
- Summary totals correctness
- Midnight rollover (simulate by adjusting lastRolloverDate in dev)
- Auto-populate preview: regenerate, swap meal, confirm fills empty only
- Add-to-plan: single day / range / multi days + replacement warning
- Swap ingredient: label present, 4 suggestions, search, regenerate, nutrition updates, persists, instructions unchanged
- Remove deletes and subtracts totals
- Free logs: checkout completion + cooknow log affect totals only
- Pro manual add affects totals only

Keep it concise and ship-ready.

────────────────────────────────────────
DELIVERABLES
────────────────────────────────────────

1) No regressions in UI layout/styling/navigation
2) Bugs fixed across totals, rollover, generation, scheduling, swaps
3) Unit tests added and passing
4) README updated with test run command
5) QA runbook file created