This is PROMPT P7 (post P1–P6) for the ReciPal codebase.

SCOPE (HARD-LOCK)
- Only modify: Planner UI controls and small Planner-context modal UX.
- Do NOT change: Recipes feed cards, Recipe Detail layout, Add-to-Plan popup (P3), Auto-populate preview overlay structure (P2), Pantry, Cart, Profile, Settings, Paywall, RevenueCat logic.
- Do NOT change: Planner totals logic, nutrition engine (P6), or swap calculation logic (P4) other than adding a missing UI close control.
- No redesigns. Only implement the changes requested.

GOALS (ALL REQUIRED)
1) Move Pro-only Manual Entry control into a chevron dropdown positioned directly BELOW the “Auto-populate Week” button.
2) Fix “Auto-populate Week” button styling to match ORANGE 3D bubble gloss CTA style (it is currently too transparent/light).
3) Auto-populate config UI: Dessert + Snackitizer toggles must control whether those categories appear in the preview/config UI.
   - Categories must remain hidden unless toggled ON.
   - After toggled ON, users must be able to change quantities/servings for those categories.
4) Planner swap popup: add a clear “Done” button to close the swap modal when swapping ingredients from within Planner day view.

────────────────────────────────────────
A) MANUAL ENTRY CONTROL: MOVE INTO CHEVRON DROPDOWN (PRO ONLY)
────────────────────────────────────────

A1) Current state:
Manual entry (Pro-only custom meal entry) exists but is placed awkwardly (previously at bottom).

A2) Required new placement:
- In Planner (Plan mode), directly BELOW the “Auto-populate Week” button, add a compact “Chevron dropdown” row control that expands/collapses.
- Label of collapsed row: “Manual Entry (Pro)” with a chevron icon.
- Only visible to Pro users (RevenueCat entitlement).

A3) Expanded content:
When expanded, show the existing Manual Entry form (do NOT redesign it):
- Name
- Calories
- Protein
- Carbs
- Fat
- Date picker (default today)
- Save
Saving behavior remains identical (writes ConsumptionLog manual_custom_entry and affects totals).

A4) Free users:
- Do NOT show this chevron row.
- No upsell here (upsell stays in blurred macro overlay from Pro gating rules).

────────────────────────────────────────
B) AUTO-POPULATE WEEK BUTTON: ORANGE 3D BUBBLE GLOSS STYLE
────────────────────────────────────────

B1) The “Auto-populate Week” button is currently too transparent/light.
Update it to:
- ORANGE 3D bubble gloss style (exact same CTA styling pattern used elsewhere for primary orange actions).
- It must be clearly visible in both light and dark mode.
- Do NOT change its position (it stays under the summary bar and above the calendar, as implemented earlier).

B2) If Pro and macros are set, the label may already be:
“Auto-populate Week (Optimized for Macros)”
Do NOT change label logic in this prompt; only styling/visibility.

────────────────────────────────────────
C) AUTO-POPULATE CONFIG: TOGGLES CONTROL CATEGORY VISIBILITY
────────────────────────────────────────

C1) Current problem:
Add Desserts / Add Snackitizers toggles exist but UI does not hide/show categories correctly.

C2) Required behavior:
In the Auto-populate preview overlay config area:
- Desserts category controls must be HIDDEN unless “Add Desserts” is ON.
- Snackitizers category controls must be HIDDEN unless “Add Snackitizers” is ON.

“Controls” includes:
- the slot/category rows in the preview layout (Desserts/Snackitizers sections)
- their serving size stepper / quantity controls

C3) After toggled ON:
- Show the category rows AND enable their serving size stepper (1–10, with “10+” display rule consistent with the rest of the app).
- The user must be able to change serving size after enabling.
- Defaults:
  - Desserts: visible only when enabled; default max generation remains 1/day (do not change generation rules here)
  - Snackitizers: visible only when enabled; default max generation remains 1/day
Do NOT change generation rules, only the UI visibility + ability to adjust servings.

C4) When toggled OFF:
- Hide that category from preview UI and config UI.
- Ensure no lingering placeholder rows remain.

────────────────────────────────────────
D) PLANNER SWAP POPUP: ADD “DONE” BUTTON (CLOSE)
────────────────────────────────────────

D1) Context:
When swapping ingredients from within Planner day view, the swap modal is missing a clear completion/exit control.

D2) Requirement:
- Add a “Done” button to the swap modal in Planner context.
- Done simply closes the modal (no extra save step).
- Keep existing swap selection behavior unchanged.

D3) Do NOT modify swap suggestion logic, search, or regenerate behavior.
Only add the Done button and ensure modal can be closed cleanly.

────────────────────────────────────────
E) VALIDATION CHECKLIST (MUST PASS)
────────────────────────────────────────

1) Manual Entry form is no longer “at the bottom” as the primary access; it is accessible via chevron dropdown under Auto-populate Week.
2) Manual Entry chevron is visible ONLY for Pro users and remains fully functional.
3) Auto-populate Week button is clearly visible and uses ORANGE 3D bubble gloss styling.
4) In auto-populate config/preview UI:
   - Desserts controls/rows are hidden unless toggled ON
   - Snackitizers controls/rows are hidden unless toggled ON
   - When toggled ON, serving controls appear and can be changed
5) Planner-context swap modal has a Done button that closes it.
6) No UI regressions in Recipes feed cards, Recipe Detail, Pantry, Cart, Profile, or Settings.

Do not mark complete unless all 6 checks pass.
