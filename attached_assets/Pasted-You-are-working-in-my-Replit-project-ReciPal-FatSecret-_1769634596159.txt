You are working in my Replit project “ReciPal”. FatSecret relay + API calls are working. The current behavior returns “healthy” recipes because the feed calls FatSecret with q: '' and our server defaults empty queries to "healthy". Those results are real FatSecret recipes, but they’re the wrong default set.

GOAL
Fix the Recipes feed behavior so:
1) “For You” shows a DIVERSE variety feed (not “healthy”), without requiring a user search query.
2) User profile preferences should be applied as a filter layer to For You (do NOT lock For You to “healthy”).
3) “Favorites” must save/unsave recipes and load them correctly when the Favorites tab is opened.
4) The Filter Menu options must map cleanly to the correct FatSecret calls/parameters (Breakfast/Lunch/Dinner/Dessert/Snacks etc), including mapping “Snackitizers” to “Snacks”.

HARD CONSTRAINTS
- Do NOT change the relay/auth plumbing (it already works).
- Do NOT change recipe card UI, layout, or existing card mapping fields.
- Make minimal changes limited to: request construction, filtering, and favorites persistence + retrieval.
- Do NOT hardcode “healthy” for empty query anywhere in the For You feed.
- Preserve existing pagination/prefetch behavior (ex: 20 cards) and any Pro gating logic.

PART A — FOR YOU FEED (Variety + preference filtering)
Current issue:
- Feed sends q: ''.
- Server defaults empty query to "healthy".
Fix:
A1) Create an explicit “variety” feed behavior that does NOT depend on q being non-empty.
    - Do NOT send an empty q that triggers the server default.
    - Instead: introduce a dedicated feed mode for For You: “VARIETY”.
    - If the FatSecret endpoint requires a search_expression/q value, use a neutral broad term that yields variety (NOT “healthy”), e.g. "recipe" or "popular" or "dinner" is too narrow. Choose the best neutral term based on our existing FatSecret endpoint response quality.
    - If our integration supports category/type params, use those instead of q.

A2) Apply user profile preferences as FILTERS after results are fetched (not as the query default).
    - Preferences examples: dietary tags, disliked ingredients, cuisine preferences, macro targets, etc.
    - Implement filtering without breaking results: fetch variety results first, then filter down.
    - Ensure we still return up to the existing limit (e.g. 20). If filtering removes too many, fetch additional pages until we fill the target count OR we reach a sane max (e.g. 3 pages).
    - Do not degrade performance with unbounded loops.

A3) “Something New” tab
    - This should feel distinct from For You.
    - If you already have a logic path: keep it.
    - Otherwise implement as: fetch variety results but change sorting/seed/page to reduce overlap with For You (e.g. different page offset, different seed term).
    - Still apply user preferences filter, but with a lighter touch if needed to maintain variety.

PART B — FAVORITES (save + load)
B1) When a user taps Favorite on a recipe card:
    - Save that recipe to a Favorites store keyed by userId.
    - Persist it so it survives refresh/log out. Prefer the project’s existing persistence layer (DB table/kv store) rather than local-only state.
B2) Favorites tab:
    - Load the saved favorites for that user and render them using the same card mapping.
    - No FatSecret call is required to render Favorites unless you explicitly need to rehydrate missing fields; if so, do it safely and lazily.
B3) Unfavorite:
    - Remove it from persistence and update UI instantly.

PART C — FILTER MENU → FATSECRET CALL MAPPING
We need the Filter Menu options to map to the correct FatSecret calls/parameters. Implement a single mapping layer:
- A dictionary/map of filterOption -> fatsecretQueryParams (or search_expression terms / category params).
- This mapping must be used consistently by For You and Something New feeds when a filter is active.

C1) Required mappings (minimum)
- Breakfast -> “breakfast”
- Lunch -> “lunch”
- Dinner -> “dinner”
- Dessert -> “dessert”
- Snacks -> “snacks”
- Snackitizers -> map to Snacks (same as “snacks”)
- Any existing filter options in the UI should be mapped. Identify them all from the Filter Menu component and implement mappings.

C2) Implementation rules
- Do NOT send q: '' ever as a default. If no filter + no search query, use the VARIETY feed behavior.
- If a filter is selected, the feed should use that filter mapping (and still apply user preferences filtering after fetch).
- If a user enters a search query, that query should take priority over filter defaults (unless UI specifies otherwise).

PART D — REMOVE / CHANGE THE SERVER DEFAULT
D1) The server defaulting empty search to “healthy” is causing the issue.
    - Update server-side logic so empty queries do NOT automatically become “healthy” for feed calls.
    - If you must keep a default for some legacy endpoint, make it conditional:
      - Only default to “healthy” for explicit “Healthy” filter, not for empty input.
      - Or only for a specific endpoint used by search bar, not the feed.

DELIVERABLES (must provide)
1) List the exact files changed.
2) Show the new mapping object for Filter Menu options and where it is used.
3) Show the For You “VARIETY” request construction and the preference-filtering function.
4) Show Favorites persistence: where it saves, where it loads, and how it updates UI.
5) Confirm the Recipes tabs behavior:
   - For You: variety, then filtered by preferences (no “healthy default”)
   - Something New: distinct variety, filtered by preferences
   - Favorites: loads saved recipes for the user

DO NOT STOP AT “EXPLANATION”
Implement changes in code and show diffs/snippets for the key parts.
