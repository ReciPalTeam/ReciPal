TITLE: ReciPal MVP Prompt 2A — FatSecret pagination plumbing + ALWAYS 20 cards on initial load (no pantry, no ranking, no tab-retap)

GUARDRAILS (follow strictly):
- Do NOT refactor unrelated code.
- Do NOT rename/move files.
- Do NOT add new libraries.
- Keep changes minimal and MVP-only.
- If something is unclear, inspect existing code and follow existing patterns.

GOAL:
Make BOTH feeds (“For You” and “Something New”) load EXACTLY 20 recipe cards on initial load, using FatSecret recipes.search.v3 pagination:
- Always request max_results=20.
- Support page_number (0,1,2,...) to retrieve subsequent pages.
- Implement a helper that can fetch multiple pages until it collects 20 displayable recipes OR hits a cap (5 pages) to avoid endless loops.
- Dedupe results by recipe_id.

IMPORTANT:
This prompt does NOT implement:
- infinite scroll append
- tab retap refresh
- pantry Have/Maybe/Need mapping
- pantry ranking
- cuisine client-side filtering
- diabetic onboarding

FILES TO INSPECT / LIKELY TOUCH:
- server/fatsecret/client.ts
- client/src/lib/recipe-store.ts
- any existing fetch wrapper used by the feed pages/components

STEP 1 — Server: ensure recipes.search.v3 supports max_results + page_number
In server/fatsecret/client.ts:
- Locate the function that calls method: 'recipes.search.v3'
- Ensure the request includes:
  - max_results: 20
  - page_number: value passed from caller (default 0 if not supplied)
- Ensure the server function signature allows page_number input.

STEP 2 — Client: implement “fetchUntil20” for initial load
In client/src/lib/recipe-store.ts (or wherever feeds fetch recipes):
- Create a helper like:
  fetchUntil20({ feedType, pageStart, baseParams }): returns { recipes: Recipe[], nextPageNumber }
- Logic:
  - Initialize collected=[] and page=pageStart
  - Loop while collected.length < 20 AND pagesFetched < 5:
    - call recipe search with page_number=page and max_results=20
    - append NEW recipes not already in collected (dedupe by recipe_id)
    - increment page
  - Return first 20 collected (or fewer if API truly can’t provide more)
  - Also return nextPageNumber = page (so later prompts can append from the correct next page)

NOTE:
- Do not invent new filter logic here.
- Whatever “displayable” means currently (e.g., existing validation checks), keep it.
- If there are no checks, treat all returned recipes as displayable.

STEP 3 — Separate state for For You vs Something New (initial load only)
- Ensure For You load and Something New load each call fetchUntil20 independently.
- Keep their recipe arrays separate (forYouRecipes vs somethingNewRecipes or similar).
- Ensure switching between feeds doesn’t cross-contaminate the arrays.

OUTPUT / DELIVERABLE:
- Provide code changes only.
- Summarize which files changed and what changed.

ACCEPTANCE TESTS (must pass):
1) Launch app → For You feed shows 20 recipe cards.
2) Switch to Something New → shows 20 recipe cards.
3) Switching back and forth does not change the other feed’s stored list unexpectedly.
4) Each feed uses max_results=20 and page_number=0 for initial load.
5) No new UI features were added beyond making initial results reliably 20.
