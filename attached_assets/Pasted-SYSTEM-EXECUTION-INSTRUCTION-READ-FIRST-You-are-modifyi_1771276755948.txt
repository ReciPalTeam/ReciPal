SYSTEM EXECUTION INSTRUCTION (READ FIRST)

You are modifying an EXISTING production codebase.
Your goal is STRICT IMPLEMENTATION — NOT redesign, NOT refactoring unrelated files, and NOT introducing new architecture beyond what is explicitly requested.

Make the SMALLEST POSSIBLE CHANGES required to:
1) Add missing instrumentation points for Unit Trace across the recipe-feed “missing ingredients → add to cart → instacart checkout” flow.
2) Add a UI toggle to enable/disable tracing WITHOUT devtools, by setting localStorage ("debug_unit_trace").

DO NOT alter existing unit normalization logic.
DO NOT alter Instacart payload logic.
DO NOT change application behavior.
DO NOT refactor.



------------------------------------------------------------
CONTEXT
------------------------------------------------------------

Unit Trace already exists from Step 1:
- src/utils/unitTrace.ts with unitTrace(), getUnitTraceBuffer(), clearUnitTraceBuffer()
- Feature gate isUnitTraceEnabled() using:
  - process.env.NEXT_PUBLIC_DEBUG_UNIT_TRACE === "true"
  OR
  - localStorage "debug_unit_trace" === "true"
- A 🧪 Unit Trace button + modal/panel exists.

Now we need to TRACE EARLIER STAGES in the PRIMARY flow:
Recipe feed card → pantry gap detection → user clicks add missing → internal cart line items → instacart mapping → checkout payload → instacart API response



------------------------------------------------------------
GOAL (STEP 2)
------------------------------------------------------------

Add the following trace events in the REAL production code paths:

A) pantry gap detection:
  EVENT: "pantry_gap_detected"

B) user action when adding missing ingredients to cart:
  EVENT: "add_missing_to_cart_clicked"

C) instacart API response (success/failure):
  EVENT: "instacart_api_response"

Also ensure correlationId is stable per ingredient/cart item across stages.



------------------------------------------------------------
CRITICAL CONSTRAINTS
------------------------------------------------------------

- ADDITIVE ONLY.
- Do NOT change any existing business logic results.
- Do NOT change unit normalization behavior.
- Do NOT change Instacart payload content beyond adding trace logs (trace must never include secrets/tokens).
- Do NOT introduce new dependencies.
- Keep code changes localized and minimal.



------------------------------------------------------------
PART 1 — ADD A SAFE UI TOGGLE (NO DEVTOOLS)
------------------------------------------------------------

Inside the existing 🧪 Unit Trace modal/panel, add:

1) A toggle switch or checkbox labeled:
   "Enable Unit Trace"

2) When toggled ON:
   localStorage.setItem("debug_unit_trace", "true")

   When toggled OFF:
   localStorage.setItem("debug_unit_trace", "false")

3) The toggle should reflect current enabled state:
   enabled := isUnitTraceEnabled() OR localStorage value

4) Changing the toggle should not require a page reload.
   (If your isUnitTraceEnabled() reads localStorage each time, you’re good.
    If not, minimally update so the UI re-renders and unitTrace() starts logging.)

This satisfies the existing feature flag requirement WITHOUT manual console steps.



------------------------------------------------------------
PART 2 — ADD TRACE EVENTS (MINIMAL INSTRUMENTATION)
------------------------------------------------------------

You must locate the actual code paths used by the app for:

1) Pantry gap detection:
   - The place where a recipe’s ingredients are compared vs pantry / missing list is computed.
   - When the app determines “missing ingredients” (the list shown to user), emit:

   unitTrace("pantry_gap_detected", {
     recipeId: <if available>,
     recipeName: <if available>,
     missingCount: <number>,
     missingIngredientsPreview: [
       { name, originalServingText, originalQty, originalUnitDisplay }
     ].slice(0, 10),
     sourceType: "recipe_feed"
   })

   IMPORTANT:
   - Do NOT generate new correlationIds here unless this is the FIRST time ingredient objects enter the shared cart pipeline.
   - If correlationIds already exist earlier, include correlationIds in preview.

2) Add missing ingredients to cart:
   - The handler for the button that adds missing ingredients to cart (whatever label it uses).
   - Emit:

   unitTrace("add_missing_to_cart_clicked", {
     recipeId: <if available>,
     recipeName: <if available>,
     itemsCount: <number>,
     correlationIds: <array if available>,
     sourceType: "recipe_feed"
   })

3) Instacart API response:
   - Find the function that calls Instacart (create cart, checkout link, etc.).
   - Immediately after the fetch/axios response (success OR error), emit:

   unitTrace("instacart_api_response", {
     ok: <true/false>,
     status: <http status if available>,
     errorMessage: <string if error>,
     correlationIds: <IDs included in that checkout attempt if available>,
     responseSnippet: <safe small subset: e.g., redirectUrl presence, ids; NEVER tokens/headers>
   })

   SAFETY:
   - Do NOT log auth headers, bearer tokens, client secrets, full payloads.
   - For snippets, only log a tiny whitelisted object.



------------------------------------------------------------
PART 3 — CORRELATION ID STABILITY
------------------------------------------------------------

Ensure correlationId remains stable for a given ingredient/cart item:

- If Step 1 attaches correlationId when ingredient enters the shared cart pipeline, reuse that.
- If earlier stages (missing list, add missing click) have access to those ingredient objects, preserve and pass through correlationId.
- If the missing-ingredient objects are different instances than the cart objects, do NOT mutate business models; instead create a parallel lookup keyed by a stable identifier (ingredient id/name+recipeId) ONLY if needed, and keep it local to the tracing layer.

DO NOT change app data models.



------------------------------------------------------------
ACCEPTANCE CHECKS (YOU MUST DO THESE IN REPLIT)
------------------------------------------------------------

1) Run the app.
2) Enable Unit Trace using the new toggle in the 🧪 Unit Trace panel.
3) From the Recipe feed (For You / Something New):
   - open a recipe
   - view missing ingredients (pantry gap)
   - click add missing ingredients to cart
   - proceed toward Instacart checkout action (as far as current app supports)
4) Confirm trace panel shows events (newest first):
   - pantry_gap_detected
   - add_missing_to_cart_clicked
   - ingredient_entered_cart_pipeline (from Step 1)
   - instacart_lineitem_mapped (from Step 1)
   - instacart_checkout_payload_ready (from Step 1)
   - instacart_api_response
5) Confirm Copy JSON still works and Clear works.
6) Confirm no UI regressions (feeds/cart/build-a-meal unchanged).



------------------------------------------------------------
OUTPUT REQUIREMENTS
------------------------------------------------------------

Return ONLY:
1) Files created or modified
2) Exact code added/changed (diff-style or full file for small files)
3) Where the toggle appears + where the 🧪 Unit Trace button appears

Do NOT summarize.
Do NOT propose improvements.
Do NOT refactor.
