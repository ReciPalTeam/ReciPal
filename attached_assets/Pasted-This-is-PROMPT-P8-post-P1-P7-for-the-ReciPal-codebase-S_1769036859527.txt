This is PROMPT P8 (post P1–P7) for the ReciPal codebase.

SCOPE (HARD-LOCK)
- Only modify: Planner tab (remove Track mode) + Profile tab (becomes Pro Dashboard) + app landing routing for Pro users.
- Do NOT change: Recipes feed cards, Recipe Detail layout, Pantry, Cart, Settings layout, Paywall screen, RevenueCat integration logic.
- Do NOT change: Planner totals rules (P1), Auto-populate overlay (P2), Add-to-Plan popup (P3), swap logic (P4), nutrition engine (P6), manual entry dropdown (P7).
- This prompt is navigation/IA and Pro dashboard wiring only.

GOALS (ALL REQUIRED)
1) REMOVE Track mode from Planner entirely (no Plan/Track toggle, no Track UI).
2) Replace any Track gating UX with the new Planner Pro entry point:
   - In Planner’s top counter area, macros remain blurred/greyed for Free users with an ORANGE “Upgrade to Pro” overlay button.
   - Pro users see full macros.
3) Profile tab becomes the Pro Dashboard when Pro is active:
   - Free users: Profile shows account/preferences + Upgrade entry points.
   - Pro users: Profile shows a stylized dashboard with daily/weekly/monthly target vs consumed comparisons + trends summary.
4) Pro landing behavior:
   - If Pro user (RevenueCat entitlement active): app opens/lands on Profile tab by default after login/app launch.
   - If not Pro: app opens/lands on Recipes tab (existing default).
5) Keep “Macros: Set / Edit” entry in Profile for Pro users (opens the macro wizard, which will be implemented in P9).

IMPORTANT NOTE
- The Macro Setup Wizard itself is NOT implemented in this prompt (that is P9).
- In this prompt, add placeholder routing hooks to open the wizard from Profile/Planner CTA.

────────────────────────────────────────
A) PLANNER: REMOVE TRACK MODE COMPLETELY
────────────────────────────────────────

A1) Remove the Plan/Track toggle UI from Planner.
- Planner should always render in “Plan” mode only.
- Remove any Track UI screens, states, or routes associated with Planner.

A2) Pro entry point in Planner (Free users):
- In the Planner top summary/counter region:
  - Calories remain visible
  - Macros remain present but blurred/greyed out
  - Add a prominent ORANGE “Upgrade to Pro” overlay button
  - Button opens Paywall screen
- Do NOT compute/store Pro-only tracking calculations for free users.

A3) Pro users in Planner:
- Macros display normally (as wired in P6)
- No overlay

A4) Ensure removing Track does NOT break:
- summary bar wiring (P6)
- auto-populate button + preview overlay (P2/P7)
- manual entry dropdown (P7)
- Cooked/Remove + midnight rollover (P1)

────────────────────────────────────────
B) PROFILE TAB: PRO DASHBOARD (PRO ONLY), FREE PROFILE OTHERWISE
────────────────────────────────────────

B1) Free user Profile
If NOT Pro:
- Show profile/preferences normally (existing)
- Keep clear upgrade entry points:
  - “Upgrade to Pro” button (opens Paywall)
- Free user can still edit preferences and restrictions.

B2) Pro user Profile becomes Dashboard
If Pro:
- Replace “Upgrade to Pro” section with:
  - “Macros: Set / Edit” button (opens Macro Setup Wizard route placeholder)
- Show stylized dashboard sections (visually appealing, consistent with brand):
  1) Today: Targets vs Consumed
     - Calories bar
     - Protein/Carbs/Fat bars
  2) This Week: Targets vs Consumed
     - Calories + P/C/F
  3) This Month: Targets vs Consumed
     - Calories + P/C/F
  4) Trends Summary
     - Simple trend insights (e.g., “Avg calories/day”, “Protein trend”)
     - Keep it positive and informational; no guilt language.

B3) Data source rules for “Consumed”
Consumed must use the same counted logic as Planner totals:
- Counted planner meals (cooked or autoCounted)
- Consumption logs: checkout_logged_recipe, cooknow_logged_recipe, manual_custom_entry
Do NOT include scheduled/uncounted meals as consumed.

B4) “Planned vs Consumed” comparisons
Dashboard should be able to show:
- Planned totals (what’s scheduled in planner for the day/week/month, scaled by servings and swaps)
- Consumed totals (counted only per rules above)
If macros are not set yet, show a friendly prompt:
- “Finish setting up macros to unlock optimized planning and tracking.”
- Button: “Set up my macros”
(Do not show the “Macros not set — planning is limited” banner here; that belongs in Planner per earlier requirements and will be wired in P9.)

B5) Visual styling constraints
- Use existing component library/theme tokens.
- Make charts/bars clean and modern, consistent with existing app style.
- No layout overhauls of the entire Profile tab—only conditional rendering for Pro dashboard sections.

────────────────────────────────────────
C) PRO LANDING ROUTING (DEFAULT TAB)
────────────────────────────────────────

C1) On app launch AND after login:
- If user is Pro (RevenueCat entitlement active), set default selected tab = Profile.
- If user is not Pro, default selected tab = Recipes.

C2) This must be resilient:
- Works after restore purchases
- Works on new device login
- Does not rely on local storage as source-of-truth; must use entitlement state.

C3) Do NOT break deep linking behavior.
- If a deep link routes to a recipe/plan, that should still override default landing.

────────────────────────────────────────
D) PLACEHOLDER ROUTES FOR MACRO WIZARD (P9 WILL IMPLEMENT)
────────────────────────────────────────

D1) Add a stable route/action handler for opening Macro Setup Wizard:
- openMacroWizard()
Used by:
- Profile “Macros: Set / Edit”
- Planner “Upgrade to Pro” flow (after upgrade success, P9 will show onboarding stack)

In this prompt:
- It can route to a placeholder “Macro Setup” screen if not yet implemented.
- Do NOT implement the full wizard here; just ensure routing is in place and non-breaking.

────────────────────────────────────────
E) VALIDATION CHECKLIST (MUST PASS)
────────────────────────────────────────

1) Planner no longer shows Plan/Track toggle or any Track UI.
2) Free users see blurred macros area with ORANGE “Upgrade to Pro” overlay button in Planner.
3) Pro users see macros normally in Planner (no overlay).
4) Profile tab:
   - Free users see normal profile + upgrade entry
   - Pro users see Dashboard with daily/weekly/monthly target vs consumed + trends summary
5) “Macros: Set / Edit” replaces Upgrade in Pro Profile and routes to macro wizard placeholder.
6) App landing:
   - Pro users land on Profile tab by default
   - Free users land on Recipes tab by default
7) No regressions in Recipes feed cards, Planner functionality, Auto-populate overlay, Add-to-Plan popup, swaps, Cart, Pantry.

Do not mark complete unless all 7 checks pass.
