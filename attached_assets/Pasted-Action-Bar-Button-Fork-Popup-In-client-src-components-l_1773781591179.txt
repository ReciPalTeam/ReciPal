Action Bar "+" Button: Fork Popup

In client/src/components/layout-shell.tsx, modify the "+" floating action button behavior:

1. Add new state variables:
   - const [forkMenuOpen, setForkMenuOpen] = useState(false);
   - const [addPantrySheetOpen, setAddPantrySheetOpen] = useState(false);

2. Change the "+" button's onClick (around line 158) from setAddMenuOpen(true) to setForkMenuOpen(true).

3. Add a new Sheet BEFORE the existing addMenuOpen Sheet. This is the fork popup:

<Sheet open={forkMenuOpen} onOpenChange={setForkMenuOpen}>
  <SheetContent side="bottom" className="rounded-t-2xl px-6 pb-8">
    <SheetHeader className="pb-4">
      <SheetTitle className="text-center">What would you like to add?</SheetTitle>
    </SheetHeader>
    <div className="flex flex-col gap-3">
      <button
        onClick={() => { setForkMenuOpen(false); setAddMenuOpen(true); }}
        className="w-full py-4 px-6 rounded-xl text-white font-bold text-base bg-gradient-to-b from-[#FF7B1A] to-[#FF6300] shadow-[inset_0_1px_1px_rgba(255,255,255,0.4),0_4px_12px_rgba(255,99,0,0.3)] border-t border-white/20 hover:opacity-90 transition-opacity"
      >
        Add Meal/Recipe
      </button>
      <button
        onClick={() => { setForkMenuOpen(false); setAddPantrySheetOpen(true); }}
        className="w-full py-4 px-6 rounded-xl text-white font-bold text-base bg-gradient-to-b from-[#34D058] to-[#22C55E] shadow-[inset_0_1px_1px_rgba(255,255,255,0.4),0_4px_12px_rgba(34,197,94,0.3)] border-t border-white/20 hover:opacity-90 transition-opacity"
      >
        Add to Pantry
      </button>
    </div>
  </SheetContent>
</Sheet>

4. In the existing addMenuOpen Sheet, change the title from "Add Entry" to "Add Meal/Recipe". Remove the "Scan Receipt" button from that grid (the third button that currently just closes the menu with no implementation).

5. After <ScanBarcodeSheet />, add: <AddPantryItemSheet open={addPantrySheetOpen} onOpenChange={setAddPantrySheetOpen} />

6. Import AddPantryItemSheet at the top: import { AddPantryItemSheet } from "@/components/add-pantry-item-sheet";

Note: The AddPantryItemSheet component doesn't exist yet — it will be created in the next prompt. For now, create a placeholder component at client/src/components/add-pantry-item-sheet.tsx that exports a simple Sheet with title "Add Pantry Item" and an empty body that accepts open and onOpenChange props.