ROLE: You are a senior engineer working in the ReciPal codebase.

PROBLEM:
The FoodGroup union type was updated to this new set:

export type FoodGroup =
  | 'Produce'
  | 'Meat & Seafood'
  | 'Dairy & Eggs'
  | 'Bread & Bakery'
  | 'Pasta, Rice & Grains'
  | 'Canned & Jarred'
  | 'Spices & Seasonings'
  | 'Oils, Sauces & Condiments'
  | 'Baking & Sweeteners'
  | 'Frozen'
  | 'Prepared Foods & Deli'
  | 'Snacks & Nuts'
  | 'Other';

BUT existing pantry items and/or classification logic still use the old FoodGroup labels and are now incorrectly categorized in the Pantry filter UI.

GOAL:
1) Update the ingredient → FoodGroup classification so EVERY pantry item maps deterministically into the NEW FoodGroup labels above.
2) Migrate/remap any EXISTING pantry items stored with old FoodGroup values into the new FoodGroup values so items show up under the correct filters immediately.
3) Do NOT break pantry overlap logic, cart, planner, or UI.

NON-NEGOTIABLES:
- Keep normalizeIngredientName() behavior intact unless a change is required for correct mapping.
- Be deterministic: same ingredient string always maps to the same FoodGroup.
- Do NOT introduce beverages.
- Do NOT refactor unrelated code.

TASKS (follow in order):

A) Locate current FoodGroup mapping/classification
- Find getIngredientFoodGroup(name) (currently in client/src/lib/demo-store.ts).
- Identify any other classification helpers (ingredient-classifier.ts may exist).
- Identify where PantryItem.foodGroup is set:
  - during addToPantry()
  - during pantry initialization / seed data
  - during any “receipt/instacart” source ingestion (even if mocked)

B) Update getIngredientFoodGroup() keyword rules to return ONLY the new FoodGroup strings
- Build keyword arrays or rules for each new group:
  1) Produce
  2) Meat & Seafood
  3) Dairy & Eggs
  4) Bread & Bakery
  5) Pasta, Rice & Grains
  6) Canned & Jarred
  7) Spices & Seasonings
  8) Oils, Sauces & Condiments
  9) Baking & Sweeteners
  10) Frozen
  11) Prepared Foods & Deli
  12) Snacks & Nuts
  13) Other
- IMPORTANT: Add strong coverage for common ingredients so placement feels obvious:
  - Flour/sugar/baking powder/vanilla/cocoa => Baking & Sweeteners
  - Rice/pasta/oats/quinoa/bread crumbs => Pasta, Rice & Grains
  - Tortillas/pita/bread/buns/naan => Bread & Bakery
  - Beans: canned beans => Canned & Jarred; dry beans/lentils => Pasta, Rice & Grains (unless you choose a different deterministic rule)
  - Oils/vinegar/soy sauce/hot sauce/salsa/dressing => Oils, Sauces & Condiments
  - Rotisserie chicken/deli turkey/pre-made salad/ready meals => Prepared Foods & Deli
  - Nuts/seeds/nut butters/trail mix => Snacks & Nuts
- Ensure no overlaps in rule order: apply in a priority sequence so one ingredient doesn’t match multiple categories incorrectly.

C) Add a one-time migration/remap for existing pantry items
- Detect pantry items whose pantryItem.foodGroup is NOT one of the new union values.
- For those items:
  - Recompute foodGroup = getIngredientFoodGroup(pantryItem.name)
  - Update the stored item in the pantry state so UI filter counts update immediately.
- This migration should run on app start / store init safely (idempotent).
- Do NOT require user action.
- Do NOT lose pantry state (have/might/gone), timestamps, or source fields.
- If there is persisted storage (localStorage) for pantry, migrate there too.

D) Update any seed/mock pantry data to use new FoodGroup values
- If demo-store initializes pantry with old FoodGroup values, update them.

E) Add a minimal verification
- Add a small test harness or dev-only console check (no external libs required) that prints counts per FoodGroup after migration, and confirm no “unknown” food groups remain.

OUTPUT REQUIRED:
1) List files edited.
2) Show exact code diffs for:
   - getIngredientFoodGroup()
   - pantry init/migration logic
   - any seed/mock pantry data updates
3) Confirm in writing:
   - existing pantry items now appear under correct new categories
   - future addToPantry() uses the new mapping
   - no beverages introduced

DO NOT:
- Change UI labels again.
- Change the FoodGroup union again.
- Refactor unrelated files.
