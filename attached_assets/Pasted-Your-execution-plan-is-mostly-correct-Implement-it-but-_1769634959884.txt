Your execution plan is mostly correct. Implement it, but with the following STRICT adjustments so we don’t break anything or bias the feed.

CRITICAL ADJUSTMENT #1 — Server default must not be global
In server/fatsecret/client.ts (line ~98) DO NOT simply change:
  search_expression: query || 'healthy'
to a different hardcoded default globally.

Instead:
- Introduce an explicit parameter/flag for the call source: feedMode (e.g. 'FEED' | 'SEARCH') or requestType.
- Only apply the previous "healthy" default when requestType === 'SEARCH' (or when a user explicitly selects a Healthy filter).
- For FEED calls, NEVER default empty to "healthy".

Concretely:
- If query is empty and requestType === 'FEED', use a dedicated variety strategy (see #2), not "healthy".
- If query is empty and requestType === 'SEARCH', return empty results or use the previous behavior (your call), but do not break search UX.

CRITICAL ADJUSTMENT #2 — “Variety” should not be a single biased term
Do NOT use 'dinner' as the neutral term. That is biased.
If you must use a single term, use 'recipe' (least biased) as fallback.

Best practice for variety feed:
- Use a rotation/seed list of broad terms and/or categories, then merge + de-dupe results:
  SEEDS = ['chicken','pasta','rice','salad','soup','tacos','stir fry','vegetarian','dessert','breakfast']
- For For You “VARIETY”: fetch 1 page from 2–3 different seeds (or 2 pages from 1 seed + 1 page from another), merge results, de-dupe by recipe_id, then take first N (20).
- Keep max fetch bounded (e.g. max 3 total upstream requests for initial load).

This ensures variety without relying on empty query defaults.

CRITICAL ADJUSTMENT #3 — Preference filtering must be “safe”
In buildForYouFeed.ts:
- Only hard-filter on attributes that are reliably available in the returned recipe objects.
- For preference fields that are not present in results, do NOT filter out (this will yield empty feeds).
- Prefer “ranking” (sorting/reordering) over “hard removal” when data quality is uncertain.

Maintain cap:
- Fill 20 items. If after filtering you have <20, fetch up to 2 additional pages (max 3 total pages/requests) then stop.

CRITICAL ADJUSTMENT #4 — Filter menu mapping must be exhaustive
Create FILTER_TO_FATSECRET mapping (yes).
But you must:
- Enumerate ALL filter options from the actual Filter Menu component and implement mappings for each (not just the ones listed).
- Map "Snackitizers" -> "snacks".
- Ensure filter selection affects both For You and Something New feed requests.

CRITICAL ADJUSTMENT #5 — Favorites persistence implementation details
Implement favorites endpoints and persistence (yes).
Requirements:
- Favorites table columns must include: user_id, recipe_id (unique per user), recipe_payload (JSON), created_at
- recipe_payload should store the exact card fields needed to render without additional FatSecret calls (title, image, macros if available, etc.).
- Favorites tab must load from DB and render immediately.
- Toggling favorite should update UI instantly (optimistic), then confirm with API.

IMPLEMENTATION CHECKLIST (deliverables required)
1) Show the exact code change in server/fatsecret/client.ts implementing requestType/feedMode and removing global 'healthy' default.
2) Show the variety seed strategy implementation and where it’s used for For You.
3) Show filter mapping object and the list of filter options you mapped (must match UI options).
4) Show favorites schema + endpoints + how the Favorites tab loads and renders saved items.
5) Confirm behavior:
   - For You: seeded variety feed + safe preference ranking/filtering
   - Something New: different seed set or offset (less overlap)
   - Favorites: DB-backed saved recipes

Proceed with implementation now and show diffs/snippets for the key files.
