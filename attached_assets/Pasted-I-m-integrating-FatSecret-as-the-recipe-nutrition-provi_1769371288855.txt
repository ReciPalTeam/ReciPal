I’m integrating FatSecret as the recipe/nutrition provider and need to map FatSecret responses into our existing ReciPal recipe card + meal card scaffolding (and later into Instacart “Create Recipe Page” line_items).

Please inspect the current codebase and return the following EXACT artifacts (no placeholders, no guesses):

1) The canonical data models / types / interfaces used for recipes:
   - Recipe (or RecipeCard / FeedRecipe / RecipeDetail / MealCardRecipe — whatever exists)
   - Ingredient object type used in recipe cards AND meal cards
   - Nutrition / Macros / Nutrients type (per-serving vs per-recipe if present)
   Provide the actual TypeScript interfaces or schema definitions (or the JS object shape if not typed).

2) The mapping/transform layers currently in use for recipe providers:
   - Any adapters, transformers, normalizers, “fromApi” functions, mappers, or parsers
   - Where recipe feed cards are built vs where recipe detail/meal cards are built
   - File paths + function names where this mapping happens

3) The exact UI components that consume recipe objects:
   - Feed recipe card component(s)
   - Recipe detail view component(s)
   - Meal planner card component(s)
   For each: file path + the props shape expected (especially required fields).

4) Ingredient/cart flow shapes:
   - The cart line item shape used when adding missing ingredients to cart
   - The payload shape currently sent at “Checkout” (even if currently mocked)
   - Any “Pantry update” function/object shape (how pantry items are stored/updated)
   Provide schemas/types and the code locations.

5) The current “filters” & preferences structure that drives recipe selection:
   - Onboarding preferences object shape
   - Sidebar filter menu state shape
   - How those filters are applied to recipe queries (where in code)
   Provide file paths and key fields.

Output format:
- First, a concise directory map of the relevant files (paths only).
- Then, paste the exact code snippets for the models/types and key mapping functions.
- Then, list any required fields that MUST be present for a feed card to render correctly.
- Do NOT refactor or change code. Just report what exists.
