The FatSecret API must ONLY be called for ingredient data — never for recipes.
Find all current FatSecret API calls and apply the following changes precisely.

════════════════════════════════════════════
PART 1: ELIMINATE ALL FATSECRET RECIPE CALLS
════════════════════════════════════════════

Remove the following FatSecret recipe calls from server/routes.ts completely:
  - searchRecipes() call at line 1357 (recipe feed)
  - getRecipeById() call at line 1385 (recipe hydration after search)
  - getRecipeById() call at line 1418 (single recipe detail fetch)

Replace them with calls to the Supabase recipe functions already in
server/lib/recipeDb.ts (searchRecipesInSupabase and getRecipeByIdFromSupabase).
server/lib/recipeDb.ts is the PRIMARY recipe data source going forward — it is
no longer a fallback.

After removing these calls, the following become dead code and must be removed:
  - searchRecipes() function from server/fatsecret/client.ts
  - getRecipeById() function from server/fatsecret/client.ts
  - fatsecretRecipeToCanonical() from server/fatsecret/adapter.ts
  - server/fatsecret/adapter.ts entirely (if no other exports remain in use)
  - The recipe-related portions of server/fatsecret/cache.ts (recipeCache,
    searchCache, getSearchCacheKey) — remove only if no longer referenced
  - client/src/lib/filter-mapping.ts — this file exists only to build
    FatSecret recipe search keyword strings. It is imported only in
    client/src/pages/recipes/index.tsx (lines 495 and 634). Remove the file
    and remove both import and call sites. Supabase queries use structured
    SQL parameters, not keyword strings — no replacement is needed for
    this file.

════════════════════════════════════════════
PART 2: THREE ALLOWED FATSECRET CALL SITES
════════════════════════════════════════════

The following FatSecret calls are ALLOWED and must remain — using searchFoods()
and getFoodById() only (ingredient functions, never recipe functions):

──────────────────────────────────────────
ALLOWED CALL SITE 1 — Swap Ingredients
──────────────────────────────────────────
Location: server/routes.ts GET /api/fatsecret/foods/search (line 1438/1454),
called by client/src/components/swap-ingredient-popup.tsx (line 50).

This is used in TWO places in the UI:
  - The "Swap" button next to each ingredient on the Recipe Detail card
  - The "Swap" button on Planner meal slots

Both trigger the same swap-ingredient-popup component which calls
/api/fatsecret/foods/search using searchFoods().

Rule: Before calling FatSecret searchFoods(), query the Supabase ingredients
table first using the search term. If sufficient results are found in Supabase,
return them without calling FatSecret. Only call FatSecret if the Supabase
results are empty or fewer than 3 matches. Our Supabase ingredient database
is small (1,706 entries) so FatSecret will be the primary source for swap
results for the foreseeable future — do not skip the FatSecret call if
Supabase has insufficient matches.

Return ONLY ingredient results — never recipe results.

──────────────────────────────────────────
ALLOWED CALL SITE 2 — Manual Entry: Build a Meal
──────────────────────────────────────────
Location: server/routes.ts GET /api/fatsecret/foods/search (line 1438/1454)
and GET /api/fatsecret/foods/:foodId (line 1463/1466), called by
client/src/components/manual-entry-sheet.tsx (lines 118 and 156).

Rule: Same as Call Site 1 — check Supabase ingredients table first. Only call
FatSecret searchFoods() if fewer than 3 Supabase matches found. getFoodById()
may be called to retrieve serving size details after a food is selected.

Return ONLY ingredient results — never recipe results.

──────────────────────────────────────────
ALLOWED CALL SITE 3 — Barcode Lookup
──────────────────────────────────────────
Location: fatsecretBarcodeLookup() in server/fatsecret/client.ts — preserve
this function entirely. It identifies food/ingredient products by barcode
and must remain in place for barcode scanning features. It returns
ingredient/food data only — this is already correct behavior.

════════════════════════════════════════════
PART 3: WHAT MUST REMAIN IN server/fatsecret/
════════════════════════════════════════════

After cleanup, server/fatsecret/ should contain ONLY:
  - searchFoods() — ingredient search (used by Call Sites 1 and 2)
  - getFoodById() — ingredient detail (used by Call Site 2)
  - fatsecretBarcodeLookup() — barcode ingredient lookup (Call Site 3)
  - fatsecretCall() — low-level API caller (needed by all above)
  - getAccessToken() — auth function (needed by fatsecretCall)
  - Any food-specific caching (foodSearchCache) — preserve if still used

Remove everything else: searchRecipes, getRecipeById,
fatsecretRecipeToCanonical, recipeCache, searchCache, getSearchCacheKey,
and the SearchFilters type if it was only used for recipe searches.

════════════════════════════════════════════
PART 4: VERIFICATION
════════════════════════════════════════════

After these changes:
  - searchRecipes() must have zero definitions and zero call sites
  - getRecipeById() must have zero definitions and zero call sites
  - fatsecretRecipeToCanonical() must have zero definitions and zero call sites
  - filter-mapping.ts must be deleted with no remaining imports
  - All recipe feed and detail routes must be served from Supabase via
    server/lib/recipeDb.ts only
  - searchFoods() and getFoodById() call sites must remain exactly at:
      /api/fatsecret/foods/search (ingredient search)
      /api/fatsecret/foods/:foodId (ingredient detail)
  - fatsecretBarcodeLookup() is preserved
  - The app must compile and start without errors