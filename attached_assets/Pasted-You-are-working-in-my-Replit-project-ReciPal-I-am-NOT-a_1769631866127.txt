You are working in my Replit project “ReciPal”. I am NOT a developer, so you must implement this safely with minimal changes and show me exactly what you changed.

GOAL
Route ALL FatSecret API calls through my DigitalOcean relay so FatSecret sees a static IP. This must apply to recipe feed loading for the Recipes page tabs:
- “For You”
- “Something New”
- “Favorites”

IMPORTANT CONTEXT
My Replit Secrets already include:
- FATSECRET_CLIENT_ID
- FATSECRET_CLIENT_SECRET
- RELAY_URL (example: http://138.197.29.205:8080)
- RELAY_KEY (a long secret string)

My relay expects requests to be sent to:
  ${process.env.RELAY_URL}/fatsecret
and requires a header:
  x-relay-key: process.env.RELAY_KEY

The relay does NOT do FatSecret authentication. It only forwards requests. Therefore:
- Keep ALL existing FatSecret OAuth/signature logic EXACTLY as-is (do not rewrite or “simplify” auth).
- Keep ALL existing request params and response parsing/mapping EXACTLY as-is.
- DO NOT change any UI components, recipe card mapping, caching behavior, pagination, filters, or tab logic.
- The only change should be WHERE requests are sent (URL) and adding x-relay-key header.

TASKS (DO THESE IN ORDER)
1) FIND all FatSecret call sites.
   - Search the repo for any of: “fatsecret”, “platform.fatsecret.com”, “rest/server.api”, “server.api”, “foods.search”, “recipes.search”, “FATSECRET_CLIENT_ID”.
   - Identify the single “lowest-level” function/helper that actually performs the HTTP request to FatSecret (fetch/axios/etc.). If multiple exist, consolidate to ONE helper used everywhere.

2) CREATE/UPDATE a single helper function (e.g., fatsecretRequest) so ALL FatSecret requests flow through it.
   - Inputs: query params object OR query string, and existing Authorization header (or however auth is currently passed).
   - Output: same response body as today (string/json) with no behavior changes.

3) CHANGE ONLY THESE TWO THINGS in the helper when making the outbound call:
   A) URL:
      - Replace direct FatSecret base URL “https://platform.fatsecret.com/rest/server.api” (or equivalent) with:
        `${process.env.RELAY_URL}/fatsecret`
      - Preserve all query parameters exactly (method, search_expression, etc.)

   B) Add the relay auth header:
      - Add header: “x-relay-key”: process.env.RELAY_KEY
      - Preserve all existing headers, especially FatSecret Authorization, exactly as they are today.

4) ENSURE all three recipe tabs (“For You”, “Something New”, “Favorites”) use the updated helper (directly or indirectly).
   - Do not modify tab logic or UI; only ensure the underlying data fetch uses the helper.

5) ADD minimal logging (server-side only) to help verify it’s using the relay:
   - Log once per request: “FatSecret via RELAY” and the FatSecret method name (e.g., foods.search).
   - Do NOT log secrets or auth headers.

6) SAFETY / FAILURE HANDLING (best practice, minimal):
   - If the relay call returns 401 with body {error:"unauthorized"} or similar, print a clear log message indicating missing/incorrect RELAY_KEY.
   - Do NOT silently change or rotate any secrets.

DELIVERABLES (SHOW ME)
- Tell me the exact file(s) you edited.
- Show a “before → after” code snippet for the helper’s outbound request line(s) (URL + headers).
- Explain how you confirmed that all FatSecret calls now go through that helper.
- Provide a simple manual test plan:
  1) Open Recipes page
  2) Click For You
  3) Click Something New
  4) Click Favorites
  and confirm no errors + recipes load.

CONSTRAINTS
- Minimal diff.
- No refactors outside the FatSecret request path.
- Do not touch database, UI components, or recipe card mapping.
- Do not introduce new libraries unless absolutely necessary.

Proceed now.
