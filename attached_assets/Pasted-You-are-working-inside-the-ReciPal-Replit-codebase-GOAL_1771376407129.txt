You are working inside the ReciPal Replit codebase.

GOAL
Fix our Instacart “checkout redirect” flow so it creates a **Shopping List Page** link (NOT a recipe page) using Instacart Developer Platform API.

WHAT’S GOING WRONG (FROM LOGS)
We are getting HTTP 401 Unauthorized. That means our request is not being authenticated correctly (missing/invalid Bearer token, wrong base URL for the key type, or calling from client-side where headers aren’t applied).

SOURCE OF TRUTH (INSTACART DOCS)
Use: POST /idp/v1/products/products_link to create a shopping list page and return a shareable link. :contentReference[oaicite:0]{index=0}
Auth: Authorization header must be “Bearer <API-key>”. :contentReference[oaicite:1]{index=1}
Dev vs Prod base URLs:
- Dev server: https://connect.dev.instacart.tools :contentReference[oaicite:2]{index=2}
- Prod server: https://connect.instacart.com :contentReference[oaicite:3]{index=3}
Body must include: title + line_items; set link_type to “shopping_list”. :contentReference[oaicite:4]{index=4}

NON-NEGOTIABLE REQUIREMENTS
1) Do NOT use the recipe page endpoint.
2) Do NOT set link_type to "recipe". Use "shopping_list".
3) The API call must happen server-side ONLY (never in browser) to keep the API key private and to ensure Authorization header is actually sent.
4) Use the correct base URL for the key:
   - If the key is a development key, call connect.dev.instacart.tools
   - If the key is a production key, call connect.instacart.com
5) Store the key in an environment variable (server only). Do not hardcode.
6) Return the generated shopping list URL from the server to the client and redirect the user to it.

IMPLEMENTATION TASKS
A) Add/verify server env var:
   - INSTACART_API_KEY must exist in Replit Secrets (server runtime env)
   - Optional: INSTACART_ENV=development|production (default development if missing)
   - Optional: INSTACART_BASE_URL override (if present, use it)

B) Create/Update a single server utility:
   - File: server/services/instacart.ts (or closest existing service file)
   - Export: createShoppingListPage(lineItems, options) -> returns { url, raw }
   - Use fetch (or axios if already used consistently) to POST:
       {BASE_URL}/idp/v1/products/products_link
     Headers:
       Accept: application/json
       Content-Type: application/json
       Authorization: Bearer ${INSTACART_API_KEY}
   - Body shape (minimum valid):
     {
       "title": "ReciPal Shopping List",
       "link_type": "shopping_list",
       "line_items": [
         { "name": "...", "quantity": <number>, "unit": "<supported unit>" , "display_text": "..." }
       ],
       "landing_page_configuration": {
         "partner_linkback_url": "<our site url>"
       }
     }
   - IMPORTANT: quantity/unit are optional per docs, but we DO send them when we have them.
   - If unit is missing/invalid, force unit="each" and quantity=1 (never send garbage units).

C) Update the API route handler that currently does Instacart redirect:
   - Find the route backing screen "/instacart" that logs:
     eventName "instacart_api_response" and "instacart_checkout_payload_ready"
   - Ensure it calls createShoppingListPage(...) with the simplifiedLineItems mapped to Instacart line_items.
   - On success:
       respond with { success:true, redirectUrl:<url> } and client redirects to it
   - On failure:
       respond with { success:false, errorMessage:<message> }

D) Fix the 401 root causes explicitly
   - Ensure Authorization header is exactly: "Bearer <API-key>"
   - Ensure you are using the correct BASE_URL for the key type:
       development -> https://connect.dev.instacart.tools
       production  -> https://connect.instacart.com
   - Ensure the request is executed server-side (node) and not in client code.
   - Add logging that NEVER prints the full API key:
       Log only first 6 chars + "...redacted"
       Log BASE_URL, endpoint path, and HTTP status code
       Log response body for non-2xx

E) Validate response parsing (critical)
   - Instacart returns a “shareable link” for the shopping list page. :contentReference[oaicite:5]{index=5}
   - Your code must robustly extract the URL from the response:
       - Inspect JSON keys returned by Instacart and capture whichever contains the generated link
       - If no link is found, throw an error: "Shopping list URL missing from Instacart response"
   - Update the “Instacart recipe page URL missing from response” error text to:
       "Instacart shopping list URL missing from response"

F) Add a quick “smoke test” endpoint (server only)
   - GET /api/instacart/health (or similar)
   - Returns:
     { hasKey: boolean, env: string, baseUrl: string }
   - DO NOT expose the key itself.

OUTPUT REQUIREMENTS
1) Make the code changes directly in the project files.
2) After changes, provide:
   - Exact file paths changed
   - The final code blocks for each changed file
   - A short runbook:
     - where to set env vars in Replit
     - how to test the endpoint
     - what logs to look for
3) Do not change any unrelated business logic (unit rounding etc.) except where required to ensure units are valid for Instacart.

EDGE CASES
- If line_items is empty: return a clear error before calling Instacart.
- If any line item name is blank: skip it and log that it was skipped.
- If Instacart returns non-2xx: include status + response text in errorMessage.

NOW DO THE WORK.
