I need you to fix four issues in the Swap Ingredient functionality across the Planner. These all relate to the SwapIngredientPopup component and how it fetches, filters, and displays ingredient alternatives. Run full end-to-end testing when complete.
Task 1: Connect FatSecret ingredient results to the Pantry for status checks
What's happening: When the SwapIngredientPopup displays ingredient alternatives from FatSecret, it is not checking the user's Pantry to see if those ingredients have a status of "Have", "Maybe", or "Gone". FatSecret results render without any "In Pantry" tag even when the ingredient exists in the user's pantry.
What should happen:

After FatSecret results are fetched and mapped to suggestion objects, cross-reference each result against the user's Pantry data
Look up each FatSecret ingredient name in the Pantry — check if it exists with a status of "Have", "Maybe", or "Gone"
If the ingredient exists in the Pantry with status "Have", display an "In Pantry" tag on that suggestion
If the ingredient exists with status "Maybe" or "Gone", do NOT show "In Pantry" — only "Have" status qualifies
This check should happen for every ingredient suggestion rendered in the popup, whether it came from FatSecret or any other source
How to investigate:
Find the SwapIngredientPopup component (likely client/src/components/swap-ingredient-popup.tsx)
Find where FatSecret results are mapped into suggestion objects — look for where inPantry is being set

It's likely hardcoded to false for FatSecret results because no pantry lookup is being done
Find how the Pantry data is stored and accessed (check for a pantry store, context, or API endpoint)
Add a pantry lookup when building the suggestion list — for each suggestion, check if its normalized name matches a Pantry item with status "Have"
Acceptance: Every FatSecret ingredient suggestion that exists in the user's Pantry with "Have" status displays an "In Pantry" tag. Items with "Maybe" or "Gone" status do not show the tag. Items not in the Pantry do not show the tag.
Task 2: Remove ALL placeholder and mock data from the Swap Ingredient suggestions
What's happening: At the bottom of the Swap Ingredient suggestion list, placeholder/mock/static ingredients are still appearing. These are hardcoded sample data entries that should not exist anywhere in the app.
What should happen:

Find and remove ALL placeholder, mock, sample, or hardcoded ingredient data from the entire codebase — not just the swap suggestions, but everywhere
The swap suggestions list should contain ONLY real data: FatSecret API results and/or ingredients from the user's actual pantry and recipe data
No static/demo ingredient arrays, no "example" entries, no fallback fake data
If FatSecret returns no results and there are no local matches, the popup should show an empty state message like "No alternatives found" — NOT placeholder data
How to investigate:

Check client/src/lib/swap-suggestions.ts — this file likely contains the static/hardcoded ingredient database that generates the placeholder suggestions
Search the entire codebase for hardcoded ingredient arrays, mock data objects, sample/demo ingredients, and static suggestion lists
Trace every source that feeds into the SwapIngredientPopup's suggestion list — remove any source that is not FatSecret or real user data
Check for fallback arrays that populate when the API returns empty — replace them with a proper empty state
Acceptance: Zero placeholder, mock, or hardcoded ingredient data exists anywhere in the app. The swap suggestion list shows only FatSecret results and real pantry data. When no results exist, a clean "No alternatives found" message displays instead.
Task 3: Fix "In Pantry" matching to use exact-match logic
What's happening: The "In Pantry" tag is appearing on ingredients that do not exactly match what's in the pantry. For example, "2% Milk" is showing "In Pantry" when only "Milk" is in the pantry. The matching is too loose — it's doing a partial/fuzzy/contains match instead of an exact match.
What should happen:
The pantry matching logic must use exact string matching (case-insensitive) — not contains, not startsWith, not fuzzy matching
"2% Milk" should NOT match "Milk" — these are different items
"Whole Milk" should NOT match "Milk"
"Milk" should ONLY match "Milk"

The comparison should be: normalizedSuggestionName.toLowerCase().trim() === normalizedPantryItemName.toLowerCase().trim()
No partial matching, no substring matching, no similarity scoring — exact match only
How to investigate:
Find the function that checks whether a suggestion matches a pantry item — it's likely using .includes(), .indexOf(), .startsWith(), or some fuzzy matching library

Replace it with a strict equality check (===) after normalizing both strings (lowercase, trim)
Test with known examples: "Milk" in pantry should only match suggestion "Milk", not "2% Milk", "Whole Milk", "Oat Milk", "Milk Chocolate", etc.
Acceptance: "In Pantry" tags only appear when the ingredient name is an exact case-insensitive match to a Pantry item with "Have" status. No partial matches. "2% Milk" does not match "Milk". "Chicken Breast" does not match "Chicken".
Task 4: Fix swap suggestions to return same-category alternatives only
What's happening: When swapping an ingredient, the suggestions are returning items from completely different categories. For example, swapping a Seasoning shows some seasonings (often just brand variants of the same spice) and then random Proteins mixed in. The suggestions should only show alternatives within the same food category.
What should happen:

When the user taps Swap on an ingredient, determine that ingredient's food category (e.g., Seasonings, Proteins, Dairy, Vegetables, Grains, Condiments, Oils/Fats, etc.)
Filter ALL suggestion results — both FatSecret and local — to only show items in the same category as the ingredient being swapped
The FatSecret search query should be refined to target the right category. Instead of searching just the ingredient name, search for alternatives within context. For example:
Swapping "Paprika" → search FatSecret for "paprika" but filter results to only Seasonings/Spices
Swapping "Chicken Breast" → filter results to only Proteins/Meats
Swapping "Cheddar Cheese" → filter results to only Dairy/Cheese
Do NOT show brand variants of the exact same ingredient as "alternatives" — if the user is swapping "Paprika", showing "McCormick Paprika" and "Great Value Paprika" is not helpful. Filter out results where the base ingredient name is the same and only the brand differs
Show genuinely different alternatives within the same category (e.g., Paprika → Smoked Paprika, Cayenne Pepper, Chili Powder, Cumin)
How to investigate:
Find how ingredient categories are determined — there may already be a classifyIngredient() function or category field on ingredient objects
Check how FatSecret results are currently filtered (or not filtered) before display
Add category-based filtering: determine the source ingredient's category, then filter all results to match that category
Add brand-variant dedup: if a result's base name (without brand prefix) matches the original ingredient, exclude it

If FatSecret doesn't return category data, use the existing classifyIngredient() function to categorize each result and filter client-side
Acceptance: Swapping a Seasoning only shows other Seasonings. Swapping a Protein only shows other Proteins. No cross-category results appear. Brand variants of the same ingredient (e.g., "McCormick Paprika" when swapping "Paprika") are filtered out. Suggestions are genuinely different alternative ingredients within the same food category.

After completing all tasks: Full E2E Testing
Pantry connection tests:

Add "Chicken Breast" to Pantry with status "Have" — open Swap on a recipe ingredient, search for chicken — confirm "Chicken Breast" shows "In Pantry" tag
Add "Milk" to Pantry with status "Have" — confirm "Milk" shows "In Pantry" but "2% Milk", "Whole Milk", and "Oat Milk" do NOT
Add "Rice" to Pantry with status "Maybe" — confirm "Rice" does NOT show "In Pantry" (only "Have" qualifies)
Add "Butter" to Pantry with status "Gone" — confirm "Butter" does NOT show "In Pantry"
Placeholder removal tests: 5. Open Swap Ingredient on 5 different ingredients — confirm zero placeholder/mock/static data appears in any suggestion list 6. Search for a nonsense term in the swap search bar (e.g., "xyzabc") — confirm an empty state "No alternatives found" message appears, NOT placeholder data 7. Search the codebase for any remaining hardcoded ingredient arrays or mock data — confirm none exist
Exact match tests: 8. With "Milk" in Pantry as "Have": confirm "Milk" = In Pantry, "2% Milk" = no tag, "Whole Milk" = no tag, "Milk Chocolate" = no tag 9. With "Chicken Breast" in Pantry as "Have": confirm "Chicken Breast" = In Pantry, "Chicken" = no tag, "Chicken Thigh" = no tag 10. With "Salt" in Pantry as "Have": confirm "Salt" = In Pantry, "Sea Salt" = no tag, "Kosher Salt" = no tag
Category filtering tests: 11. Swap a Seasoning (e.g., Paprika) — confirm all suggestions are Seasonings/Spices, no Proteins or Dairy appear 12. Swap a Protein (e.g., Chicken Breast) — confirm all suggestions are Proteins/Meats, no Seasonings or Grains appear 13. Swap a Dairy item (e.g., Cheddar Cheese) — confirm all suggestions are Dairy/Cheese items 14. Swap a Seasoning — confirm no brand variants of the same spice appear (no "McCormick Paprika" when swapping "Paprika") 15. Verify suggestions are genuinely different alternatives, not just the same ingredient with a different brand name
General: 16. Test Swap Ingredient from the main Planner meal card — confirm all four fixes work 17. Test Swap Ingredient from inside the Swap Recipe preview card — confirm identical behavior 18. Check the browser console for errors during all flows
Report back with test results for each scenario.