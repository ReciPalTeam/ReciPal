You are modifying an existing Replit project currently named “Macrocart” that contains a working macronutrient calculator/tracker. You MUST preserve all macro tracker functionality and move it behind a Pro subscription entitlement.

This project is a web application that will be wrapped with Capacitor and distributed as native iOS and Android apps. App Store compliance is mandatory. Do not deviate from these requirements.

ABSOLUTE RULES (NON-NEGOTIABLE)
1) Do NOT invent or add navigation items outside what is specified. No extra tabs (especially no “Grocery” tab).
2) UI must be app-like (tabs + stack navigation). It must NOT resemble a single long scrolling webpage.
3) All Pro access MUST be controlled by RevenueCat entitlement state (server-validated via RevenueCat SDK). Do NOT use local storage or client-only flags as the source of truth.
4) Because we do not have API keys yet, implement placeholders and a full demo mode (app usable end-to-end without keys).
5) Rename/rebrand everything to “ReciPal”.

REBRAND / NAMING (MANDATORY)
- Replace “Macrocart” naming with “ReciPal” across:
  - App title, logo text, headers, login screen, nav labels, metadata, README
- The app’s core identity is meal planning + ordering by recipe, not macro tracking.

COLOR SYSTEM (MANDATORY)
Integrate these with the existing Macrocart primary green (extract the current hex from the repo’s theme/styles):
- Orange: #ff6300
- Deep Green: #1c502d
- Existing Macrocart green becomes: --recipal-primary-green

Create a single theme system (CSS variables or Tailwind config) with:
- --recipal-primary-green: (existing)
- --recipal-deep-green: #1c502d
- --recipal-orange: #ff6300

Usage constraints:
- Primary Green: primary CTAs, active tab highlight
- Deep Green: headings, top bars, strong accents
- Orange: secondary CTAs and highlights (e.g., “Add to Plan”); do not overuse

NAVIGATION ARCHITECTURE (HARD-LOCK)
Bottom tab bar MUST have exactly 5 tabs in this exact order:
1) Recipes
2) Planner
3) Pantry
4) Cart
5) Profile

DO NOT add any other tabs.

Top bar behavior:
- Right side: Hamburger menu icon (global on all tabs)
- Left side: Filter icon (only on Recipes + Pantry + Favorites toggle screens; not required on Profile unless used)
- Recipes tab includes a search bar at top.

HOME ROUTING RULE
- Free users land on Recipes tab by default after login.
- Pro users land on Profile tab by default after login (Profile becomes their “home dashboard”).
- Recipes must still be accessible for Pro users in bottom tab bar.

HAMBURGER MENU (GLOBAL, RIGHT ICON) — MUST INCLUDE:
- Settings (opens Settings Hub screen)
- Upgrade to Pro (opens Paywall)
- Restore Purchases (also accessible inside Settings Hub)
- Manage Subscription (also accessible inside Settings Hub)
- Notification Preferences (also accessible inside Settings Hub)
- Privacy Policy (external URL)
- Terms of Service (external URL)
- Nutrition Disclaimer screen/link
- Affiliate Disclosure screen/link (if applicable placeholder)
- Contact Support (mailto or form)
- Logout

SETTINGS HUB SCREEN (APP STORE REQUIRED)
Create a dedicated Settings screen accessible from hamburger menu. This MUST contain:
- Restore Purchases button (mandatory)
- Manage Subscription button (mandatory)
- Notification Preferences (meal reminders, grocery reminders, promotional toggle)
- Privacy Policy link (hosted URL placeholder)
- Terms of Service link (hosted URL placeholder)
- Delete Account (mandatory, in-app)
- Contact Support
- Nutrition disclaimer (“not medical advice”)
- Affiliate disclosure placeholder (if affiliate revenue exists)
- App version string

ACCOUNT SYSTEM (STORE COMPLIANCE)
- Keep existing auth/login.
- Must support email/password login, password reset, session persistence.
- MUST implement in-app Delete Account:
  - accessible from Settings Hub
  - deletes the user account AND deletes or anonymizes associated personal data
  - confirm completion to user
- Ensure deletion behavior is consistent with a privacy policy (use placeholder URLs now, but implement the deletion function).

SUBSCRIPTIONS (REVENUECAT) — ARCHITECTURE MUST BE BUILT NOW
The Pro tier is a $4.99/month auto-renewing subscription.
- Integrate RevenueCat SDK now.
- Pro access must be controlled by RevenueCat entitlement status.
- Do NOT use local storage as truth for Pro.
- Create a Paywall screen with:
  - Pro features list
  - price ($4.99/month)
  - required disclosures: auto-renewing, cancel anytime, renewal terms
  - buttons that trigger platform-native purchase (RevenueCat)
  - Restore Purchases button present somewhere (in Settings Hub is required; optional on Paywall too)
  - links to Privacy + Terms (mandatory)
- Add Manage Subscription button that opens platform subscription management UI.

NOTE: If running as web-only in Replit, mock the platform-specific calls but keep the RevenueCat architecture and interfaces so it works when wrapped with Capacitor.

PUSH NOTIFICATIONS (ARCHITECTURE HOOKS NOW)
We will implement native push via Capacitor (APNs/FCM). For now:
- Create Notification Preferences screen with toggles:
  - Meal reminders
  - Grocery reminders
  - Promotional notifications
- Include a pre-permission explainer screen design:
  - explains why notifications are used and what types will be sent
- Do not implement web push.
- Implement placeholders/hooks for Capacitor push later.

DEEP LINKING (ARCHITECTURE HOOKS NOW)
Add routing support (even if mocked) for:
- recipal://recipe/:id
- recipal://planner
- recipal://cart
- recipal://paywall
- recipal://profile
Also support share links that open the recipe snapshot screen.

CORE UX SCREENS (CREATE THE STRUCTURE NOW)
You must create these screens/routes and wire them to nav:

TAB: Recipes
- RecipesRoot screen with:
  - Top search bar
  - Left filter icon
  - Right hamburger menu
  - Segmented toggle (hard-locked): “For You” | “Something New” | “Favorites”
  - Filter sheet accessible via filter icon

TAB: Planner
- PlannerRoot screen:
  - One-week-only planner (NO multi-week UI in Prompt 1; just structure)
  - View toggle: “Card View” | “List View”

TAB: Pantry
- PantryRoot screen with:
  - State filters: Have | Might Have | Gone/Expired
  - Food group filter
  - Multi-select mode with Select All and Delete All controls

TAB: Cart
- CartRoot screen with exactly 3 sections in this order:
  1) “Your Most Popular Buys — Buy Again”
  2) “Your Cart” (deduped list)
  3) “Add-ons”
- Checkout with Instacart CTA button (placeholder)

TAB: Profile
- ProfileRoot screen:
  - For Free: profile + preferences summary + Upgrade CTA
  - For Pro: Pro Dashboard becomes “home” (macro summary, weekly totals, quick links)

RECIPE DETAIL SCREEN (SHARED)
Create a shared RecipeDetail screen (opens from Recipes/Favorites/Planner):
Must include:
- Image + Title
- Ingredients list
- Cooking steps
- Nutrition summary
- Pantry overlap indicators next to ingredients
- CTAs:
  - Cook Now
  - Add To Plan
  - Save to Favorites
  - Share (deep link snapshot)
  - Get Missing Ingredients (adds to cart)

SHARE SNAPSHOT SCREEN (REQUIRED)
A share action must generate a shareable link that opens a “Recipe Snapshot” page showing what the user sees (recipe + nutrition summary). No SMS provider. Web links only.

PROFILE BUILDER (ONBOARDING) — REQUIRED STRUCTURE
After signup/first login, show Profile Builder wizard with these questions:
1) Allergies (multi-select):
Peanuts, Tree nuts, Shellfish, Fish, Dairy, Eggs, Soy, Gluten, Sesame, Other
2) Dietary preferences (multi-select):
None, Vegetarian, Vegan, Pescatarian, Halal, Kosher, Dairy-free, Gluten-free, Low-carb
3) Cooking comfort (single):
Quick & easy, Comfortable following recipes, I enjoy more involved cooking
4) Cost preference (single):
Keeping costs low, A balance of cost and quality, I’m flexible
5) Missing tools (multi-select):
Oven, Blender, Food processor, Air fryer, Instant Pot/pressure cooker, Stand mixer
Footer on every step: “You can change these anytime in Settings.”

DATA MODELS (DEFINE NOW)
Implement/extend data models to support architecture:
- User: allergies[], dietaryPreferences[], cookingComfort, costPreference, missingTools[], languagePreference, and RevenueCat customer/entitlement identifiers
- Recipe: id, title, image, cookingStyle, mealTypes, ingredients[], steps, nutritionSummary
- PlannerWeek: userId, weekStartDate, meals by day + meal type (one week only)
- PantryItem: userId, name, normalizedName, foodGroup, state (have/might/gone), lastUpdated, source
- CartItem: userId, normalizedName, displayName, quantity, unit, sourceRecipes[], isAddon boolean, isBuyAgain boolean
- Favorites: userId, recipeId

ENVIRONMENT VARIABLES (PLACEHOLDERS + DEMO MODE)
Add env vars (empty placeholders) and README docs:
- SPOONACULAR_API_KEY=""
- OPENAI_API_KEY=""
- INSTACART_API_KEY=""
- REVENUECAT_PUBLIC_KEY=""
- REVENUECAT_ENTITLEMENT_ID="pro"
- PRIVACY_POLICY_URL=""
- TERMS_URL=""
- SUPPORT_EMAIL=""
- BASE_URL=""

DEMO MODE REQUIREMENT
Because keys are missing, implement demo mode that:
- Uses mock recipes (>=30) with mealTypes including Snacks and “cookingStyle”
- Allows Planner to add recipes
- Allows Cart to populate and dedupe (basic)
- Allows Pantry to add items manually
- Paywall shows UI and mocks purchase state (but does NOT fake entitlement as source of truth; instead use a stub service that imitates RevenueCat calls so later replacement is straightforward)

LOADING / ERROR / OFFLINE STATES (STRUCTURE NOW)
- Every async screen must have loading states.
- Provide a global error boundary UI.
- Provide basic offline state screen or toast (placeholder).

DELIVERABLES FOR THIS PROMPT (DO NOT SKIP)
1) ReciPal rebrand applied across app
2) Bottom tab nav EXACTLY matches 5 tabs specified
3) Hamburger menu exists on the RIGHT and contains all required items
4) Settings Hub screen exists with Restore/Manage/Delete/Legal/Support
5) RevenueCat integration scaffold exists and Pro entitlement gating uses RevenueCat service layer
6) Paywall screen exists with required disclosures and links
7) Screen skeletons exist for Recipes/Planner/Pantry/Cart/Profile and shared Recipe Detail + Recipe Snapshot
8) Demo mode works without API keys
9) No “Grocery” tab exists anywhere
