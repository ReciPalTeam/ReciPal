This is PROMPT P10.1 (navigation-only hotfix) for the ReciPal codebase.

SCOPE (HARD-LOCK)
- Navigation change ONLY.
- Do NOT change Profile page state, layout, content, Pro dashboard, macro wizard links, or any Profile logic.
- Do NOT change Planner, Recipes, Pantry, Cart screens or their logic.
- Do NOT change RevenueCat/paywall behavior.
- Do NOT change styling across the app except for adding ONE new hamburger menu item row.
- Goal is ONLY to remove Profile from bottom tab bar and add it to the hamburger menu.

GOAL
1) Remove “Profile” tab/icon/label from the bottom navigation bar.
2) Add a new “Profile” menu item inside the hamburger menu (top menu) that navigates to the existing Profile page exactly as-is.
3) Preserve Pro landing behavior:
   - If Pro user: app should still land on the Profile/Dashboard view on launch/login.
   - If not Pro: app should land on Recipes (existing default).
   IMPORTANT: This routing must still work even though Profile is no longer a bottom tab.
4) Do not break deep links or existing routes.

────────────────────────────────────────
A) BOTTOM NAV UPDATE (REMOVE PROFILE)
────────────────────────────────────────

A1) Update bottom tab bar to include ONLY these items (no Profile):
- Recipes
- Planner
- Pantry
- Cart

A2) Ensure tab indices, routing, and selected state logic are corrected so no off-by-one bugs occur.
A3) Ensure any icons/labels for Profile are fully removed from bottom nav.

────────────────────────────────────────
B) HAMBURGER MENU UPDATE (ADD PROFILE ITEM)
────────────────────────────────────────

B1) In the hamburger/side menu list, add a menu item:
- Icon: use the existing Profile/user icon already used in the app (do not introduce a new icon set)
- Label: “Profile”

B2) Place “Profile” near the top of the menu (top 1–3 items).
B3) On click, navigate to the existing Profile page route/screen component.
- Do NOT recreate the Profile page.
- Do NOT change its internal state, tabs, Pro dashboard rendering, or any copy.

────────────────────────────────────────
C) PRO LANDING ROUTE (MUST STILL LAND ON PROFILE)
────────────────────────────────────────

C1) Maintain logic:
- If RevenueCat entitlement active (Pro): on app launch/login, navigate to Profile page route.
- If not Pro: navigate to Recipes.

C2) Since Profile is no longer a tab:
- Implement Pro landing as a route navigation action (e.g., navigate("/profile")) rather than selecting a bottom tab index.
- Ensure this does NOT require the hamburger menu to be opened; it should land directly on the Profile page.

C3) Ensure back navigation works naturally:
- If user lands on Profile, back should return to Recipes (or last visited tab) without trapping the user.

────────────────────────────────────────
D) REGRESSION SAFETY CHECKS
────────────────────────────────────────

D1) Confirm the Profile page is unchanged visually and functionally.
D2) Confirm the hamburger menu still includes all prior items unchanged, plus the new Profile item.
D3) Confirm bottom nav still works correctly across all remaining tabs.
D4) Confirm deep links to Profile still work (if implemented).
D5) Confirm Pro landing still works without relying on a bottom nav Profile tab.

────────────────────────────────────────
E) VALIDATION CHECKLIST (MUST PASS)
────────────────────────────────────────

1) Profile tab/icon/label no longer appears in bottom navigation.
2) Bottom navigation has exactly: Recipes, Planner, Pantry, Cart.
3) Hamburger menu has a “Profile” item near the top with a profile/user icon.
4) Clicking “Profile” in hamburger opens the existing Profile page unchanged.
5) Pro users still land on Profile on launch/login.
6) Free users still land on Recipes on launch/login.
7) No regressions to any screen logic or UI.

Do not mark complete unless all 7 checks pass.
