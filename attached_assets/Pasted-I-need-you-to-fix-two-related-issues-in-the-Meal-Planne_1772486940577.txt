I need you to fix two related issues in the Meal Planner, then run full end-to-end testing when done. Both involve the recipe cards in the Planner view.
Task 1: Fix "Swap Recipe" popup inconsistency
What's happening: The "Swap Recipe" button on meal cards in the Planner opens a popup/modal, but it behaves differently depending on the meal card:

Some cards load the Swap Recipe window correctly (search bar + recipe list displayed properly)
Some cards load the window stretched too wide, breaking the layout
Some cards only show a search bar with no recipe results underneath
What should happen: Every meal card's "Swap Recipe" button should open the same consistently-sized and styled modal, with a search bar at the top and a scrollable list of replacement recipe options below it. This should work identically regardless of whether the meal is locked or unlocked.
How to investigate:

Find the Swap Recipe modal/popup component and trace how it receives its data (recipe list, dimensions, styling)
Check if different meal types, locked states, or data conditions cause the modal to render differently — there may be a conditional branch or missing data guard causing the inconsistency
Look for CSS/layout issues — the modal may not have fixed or max-width constraints, or its container may inherit different widths from parent elements
Check if the recipe list fetch is failing silently for some meals, leaving only the search bar visible
Ensure the modal dimensions and recipe list population are independent of the meal card's lock state
Fix requirements:

The Swap modal must have consistent fixed dimensions (max-width) regardless of which meal card triggers it
The recipe list must always load — add proper loading states and error handling if the fetch fails
Locked and unlocked meals must both open the same Swap modal with the same behavior
Task 2: Replace "Swap" with "Undo" button when meal is marked Cooked
What should happen: When a user marks a meal as "Cooked":

The "Swap" button should disappear completely
A red "Undo" button should appear in its place
The "Undo" button must be styled identically to the existing "Cooked" and "Swap" buttons — same size, same 3D bubble/gloss effect, same border-radius, same padding — just with a red color scheme
Tapping "Undo" reverts the meal from "Cooked" back to its previous state, and the "Swap" button reappears
Logic: A Cooked meal means it has been eaten — the user should not be able to swap it for a different recipe. The only action available is to undo the Cooked status.
Implementation guidance:
Find the existing button component used for "Cooked" and "Swap" — reuse the same component/styles for the "Undo" button
The 3D bubble gloss effect should match exactly, just with a red color (background, shadow, highlight)

Conditional rendering: if meal.cooked === true (or however the cooked state is tracked), render the "Undo" button instead of "Swap"
The "Undo" button should call whatever function reverts the cooked status, then re-render the card with "Swap" visible again
After completing both tasks: Full E2E Testing
Test every combination across the Planner:

Open Swap modal on 5+ different meal cards across different meal types (Breakfast, Lunch, Dinner, Dessert) — confirm the modal renders identically every time with correct dimensions, search bar, and recipe list
Swap a recipe on an unlocked meal — confirm the swap works and the new recipe displays
Swap a recipe on a locked meal — confirm the same modal appears and swap works the same way
Mark a meal as Cooked — confirm the Swap button disappears and a red Undo button appears with matching 3D gloss styling
Tap Undo on a Cooked meal — confirm it reverts to the uncooked state and the Swap button reappears
Verify that a Cooked meal cannot be swapped (no Swap button visible)
Test on multiple screen sizes if possible to confirm the Swap modal doesn't stretch or break on different viewports
Report back with test results for each scenario.