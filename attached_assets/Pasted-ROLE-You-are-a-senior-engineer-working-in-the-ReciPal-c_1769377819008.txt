ROLE: You are a senior engineer working in the ReciPal codebase.

PROBLEM (CURRENT BUG):
FoodGroup labels were updated, but pantry items are NOT being reclassified, so:
- Ingredient cards still show old/wrong groups (or blank/mismatched)
- The Pantry “Food Groups” filter menu is NOT populated correctly (ingredients aren’t showing under groups / counts are wrong / selecting a group doesn’t filter correctly)

GOAL:
1) Reclassify ALL pantry items into the CURRENT FoodGroup union (listed below).
2) Ensure every Pantry ingredient card displays the correct FoodGroup.
3) Ensure the Food Groups filter menu is driven from the reclassified pantry items and filters the pantry list correctly.
4) Fix persistence: after refresh, classification still correct.

CURRENT FoodGroup union (DO NOT CHANGE):
export type FoodGroup =
  | 'Produce'
  | 'Meat & Seafood'
  | 'Dairy & Eggs'
  | 'Bread & Bakery'
  | 'Pasta, Rice & Grains'
  | 'Canned & Jarred'
  | 'Spices & Seasonings'
  | 'Oils, Sauces & Condiments'
  | 'Baking & Sweeteners'
  | 'Frozen'
  | 'Prepared Foods & Deli'
  | 'Snacks & Nuts'
  | 'Other';

NON-NEGOTIABLES:
- Do NOT refactor unrelated files.
- Do NOT change normalizeIngredientName() behavior unless required for correctness.
- Do NOT change UI labels or FoodGroup union.
- Beverages are excluded.
- Mapping must be deterministic: same ingredient string => same FoodGroup.
- All pantry items MUST end with a FoodGroup value from the union above (no legacy strings).

TASKS (FOLLOW EXACTLY):

A) FIND WHERE THE WRONG GROUPS COME FROM
1) Locate:
   - PantryItem type and where PantryItem.foodGroup is set (demo-store.ts)
   - getIngredientFoodGroup(name) (demo-store.ts)
   - any pantry seed/mock data that hardcodes foodGroup
   - any persistence hydration (localStorage or similar) that loads pantry items
2) Identify whether the filter menu is:
   - hardcoded list, OR
   - derived from pantry items, OR
   - derived from FoodGroup union, OR
   - broken because items contain legacy group strings

B) FIX CLASSIFICATION FUNCTION (MUST RETURN ONLY NEW GROUP VALUES)
1) Update getIngredientFoodGroup(name) so it ONLY returns one of the union values above.
2) Implement keyword coverage for each group and a priority order to avoid overlap.
   - Must correctly bucket common ingredients:
     - flour/sugar/baking powder/vanilla/cocoa => Baking & Sweeteners
     - pasta/rice/oats/quinoa/couscous => Pasta, Rice & Grains
     - bread/tortilla/pita/buns/naan => Bread & Bakery
     - canned tomatoes/canned beans/jarred olives => Canned & Jarred
     - olive oil/vinegar/soy sauce/hot sauce/dressing/ketchup/mayo => Oils, Sauces & Condiments
     - cumin/paprika/garlic powder/seasoning blends/salt/pepper => Spices & Seasonings
     - deli turkey/rotisserie chicken/pre-made salad/prepared meals => Prepared Foods & Deli
     - nuts/seeds/nut butter/trail mix => Snacks & Nuts
     - frozen veg/frozen fruit/frozen shrimp => Frozen
     - produce fresh items => Produce
     - animal proteins => Meat & Seafood
     - dairy and eggs => Dairy & Eggs

C) ADD AN IDEMPOTENT MIGRATION ON APP START (CRITICAL)
1) On store initialization / hydration:
   - Iterate through all pantry items.
   - If pantryItem.foodGroup is missing OR NOT one of the allowed union values:
       set pantryItem.foodGroup = getIngredientFoodGroup(pantryItem.name)
   - If pantryItem.foodGroup IS already valid, leave it unchanged (idempotent).
2) Persist the migrated pantry back to storage (if used), so refresh keeps correct groups.
3) Ensure migration runs once per startup and does not cause infinite loops.

D) ENSURE PANTRY CARDS DISPLAY THE GROUP
1) Locate pantry item card UI (Pantry list component/page).
2) Confirm it renders pantryItem.foodGroup on the card (or add it in a minimal way).
3) Do NOT redesign the card—just ensure the correct group label is visible and correct.

E) FIX FOOD GROUP FILTER MENU TO CONNECT TO PANTRY ITEMS
1) The filter menu must be derived from pantry items AFTER migration:
   - Build a list of groups that actually exist in pantry (counts per group).
   - Display groups with counts (optional but recommended).
2) Selecting a group must filter the pantry list by pantryItem.foodGroup.
3) “All” should show all items.
4) If menu currently uses old group strings or a hardcoded old list, replace with the derived list.
5) Ensure it works even if a group currently has 0 items (either hide or show disabled — choose ONE consistent behavior).

F) QUICK VERIFICATION (DEV-ONLY OK)
Add a small dev console check after migration:
- Print counts by foodGroup
- Confirm no unknown/legacy groups remain

OUTPUT REQUIRED:
1) List files edited.
2) Provide exact code diffs for:
   - getIngredientFoodGroup()
   - migration logic (store init/hydration)
   - filter menu logic (where it derives groups and filters)
   - pantry card display (if changed)
3) Confirm with a short checklist:
   - pantry items are reclassified
   - cards show correct group
   - filter menu shows groups from pantry and filters correctly
   - refresh persists corrected groups
