You are working in the Recipal Replit project. We need to fix the Macro Wizard + Macro Calculator logic for the “Build muscle” goal.

CURRENT PROBLEM
The “Build muscle” macro outputs are carb-dominant (roughly ~16–17% protein, ~66% carbs, ~17–18% fat). Example output currently: 3268 calories, 139g protein, 539g carbs, 62g fat. This implies the macro split is driven by static percentages (like 0.16/0.66/0.18) instead of evidence-based protein-first logic.

GOAL
Keep the existing TDEE/BMR + activity multipliers exactly as-is.
Only change the macro allocation and add two low-friction questions that help determine whether protein-forward vs carb-forward macros are appropriate.

TASKS (DO THESE IN ORDER)

1) LOCATE THE MACRO CALCULATOR LOGIC
Search the codebase for the macro calculation function used by the Macro Wizard results page.
Look for keywords: “macro”, “protein”, “carbs”, “fat”, “TDEE”, “BMR”, “activity”, and any ratio constants like 0.66, 0.18, 0.16.
Identify the function(s) that produce protein_g, carbs_g, fat_g for the “Build muscle” goal.

2) ADD 2 NEW REQUIRED QUESTIONS TO THE MACRO WIZARD (MINIMAL FRICTION)
Add these two single-select steps after Activity Level (or right before the results screen):

A) Training Style (required)
Prompt: “What best describes your training?”
Options:
- Strength/Hypertrophy focused (heavy lifting, moderate volume, longer rests)
- Mixed training (lifting + conditioning/sports)
- Endurance/Conditioning focused (HIIT/cardio/sports circuits)
Store as: trainingStyle = "strength" | "mixed" | "endurance"

B) Priority (required)
Prompt: “What’s your priority right now?”
Options:
- Maximize lean muscle gain (lean as possible)
- Balanced muscle + performance
- Performance & training output
Store as: priority = "lean_gain" | "balanced" | "performance"

Persist these values in the wizard state, include them in whatever data structure/payload is used to calculate macros, and ensure the results screen uses them.

3) REPLACE “BUILD MUSCLE” MACRO ALLOCATION WITH PROTEIN-FIRST LOGIC
For Build muscle only, replace static percentage splits with this protein-first approach:

Inputs: calories (from TDEE logic), weightLb, trainingStyle, priority

Step 1: Protein (grams anchored to bodyweight)
Use protein g/lb multipliers based on trainingStyle + priority:

- strength + lean_gain → 1.1 g/lb (default)
- strength + balanced → 0.9 g/lb
- strength + performance → 0.9 g/lb
- mixed + lean_gain → 0.9 g/lb
- mixed + balanced → 0.85 g/lb
- mixed + performance → 0.85 g/lb
- endurance (any) → 0.8 g/lb (default)
Also enforce a minimum floor of 0.8 g/lb if needed by safety clamps.

protein_g = weightLb * multiplier
protein_cal = protein_g * 4

Step 2: Fat (stable range + minimum floor)
Default fatPctTarget = 0.25 (25% calories), allowed 0.20–0.30
Fat minimum floor: fat_g_min = 0.30 g/lb

fat_g_from_pct = (calories * fatPctTarget) / 9
fat_g = max(fat_g_from_pct, fat_g_min)
fat_cal = fat_g * 9

Step 3: Carbs (remainder)
carb_cal = calories - protein_cal - fat_cal
carbs_g = carb_cal / 4

Step 4: Safety clamps (must)
If carb_cal < 0:
- first reduce fatPctTarget to 0.20 (still keep fat floor), recalc carbs
If still carb_cal < 0:
- reduce protein down toward minimum 0.8 g/lb (do not go below), recalc carbs
Never return negative carbs. Clamp carbs_g to >= 0.

Round final grams to whole numbers for display.

4) ADD OPTIONAL RESULT EXPLANATION (IF EASY)
On the results screen, add a short explanation line based on branch:
- strength + lean_gain → “Your macros are protein-forward to support lean muscle gain with minimal fat gain.”
- endurance → “Your macros prioritize carbs to support training output and recovery.”

5) VALIDATE WITH A SANITY TEST
After implementing, test this exact flow in the wizard:
Goal: Build muscle
Sex: male
Height: 5’10
Weight: 170 lb
Activity: very active
TrainingStyle: strength
Priority: lean_gain
Calories should remain whatever your TDEE logic computes (currently ~3268 in our example), but macros should no longer be carb-dominant. Protein should be closer to ~187g (170*1.1), fat should be >= max(25% calories or 0.3g/lb), and carbs should be the remainder.

DELIVERABLE
Commit changes that:
- add the 2 new wizard steps + state
- update build muscle macro allocation to protein-first logic
- keep TDEE calculation unchanged
- show correct macros on results screen
