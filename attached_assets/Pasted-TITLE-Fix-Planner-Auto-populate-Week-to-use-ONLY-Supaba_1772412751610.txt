TITLE
Fix Planner → Auto-populate Week to use ONLY Supabase recipes (remove all placeholder usage) and ensure user preference architecture drives both “For You” + “Auto-populate Week”, then run existing E2E tests.

MODE
Plan Mode FIRST. Do NOT implement yet.
Output required in Plan Mode:
1) Root-cause analysis (what’s currently feeding placeholder recipes)
2) Proposed plan (numbered tasks, smallest diffs)
3) File list you will inspect + why
4) Acceptance criteria checklist (objective tests)
5) Risks/edge cases + how you’ll validate

CONTEXT (what you must assume)
- The Planner section has: “Auto-populate Week” within “Preview Your Week” modal/window.
- Current behavior: Auto-populate Week is NOT pulling from Supabase “recipes” table; it’s using old placeholder data.
- Requirement: placeholder recipes were previously instructed to be removed entirely from the codebase; if any remain (seed JSON, mock arrays, test fixtures used in prod paths, etc.) they must not be used by runtime logic.
- The app has an existing “user preferences” architecture and it MUST drive:
  (a) For You page recipe selection
  (b) Auto-populate Week recipe selection
- The fix must ensure one consistent source-of-truth for recommendations/selection logic so these two features do not diverge.
- After implementation, you MUST run the existing built-in E2E test suite and report results (pass/fail + any fixes applied).

SCOPE
IN SCOPE:
- Fix Auto-populate Week so it queries Supabase recipes only.
- Remove/disable any placeholder recipe sources still used by runtime code paths.
- Ensure preferences architecture applies consistently to For You and Auto-populate Week.
- Refactor minimally to unify selection logic (shared service/hook/util) if needed.
- Run existing E2E tests and fix any failures caused by the change (within scope).

OUT OF SCOPE:
- UI redesign or major component rewrites.
- Changing Supabase schema unless absolutely necessary (and if you think it is, stop and ask before changing schema).
- Adding new preference types unless required to make existing preferences actually apply.
- Creating a brand-new E2E framework; use only the existing established tests.

REQUIREMENTS (crisp + testable)
Functional:
1) Auto-populate Week must fetch candidate recipes from Supabase (not local arrays, not mock JSON).
2) If Supabase returns zero recipes, Auto-populate Week must show a clear, non-crashing empty state (no placeholders).
3) For You and Auto-populate Week must use the same preference-driven filtering/ranking logic (single shared entry point).
4) Preferences must be applied deterministically (same inputs → same ranked set), with optional randomness only after ranking and with safeguards against repeats.

Data / Preferences:
- Identify the current preference data source (e.g., user_profile table, preferences table, local user settings synced, etc.) and the current logic that applies it.
- Ensure BOTH features call the same function/service/hook to apply preferences.
- Ensure any preference exclusions (allergies, dislikes, cuisines, diet tags, equipment, etc.) are respected in both places.

E2E Testing:
- Identify the established E2E runner in this repo (e.g., Playwright/Cypress/etc.) and the exact command(s) used in this project.
- After implementing changes, run the E2E suite locally in Replit using the repo’s documented commands.
- If E2E tests fail due to legitimate behavior changes, fix the implementation (preferred) or update tests ONLY if they are asserting placeholder behavior (and only then).

Security / Reliability:
- No secrets committed.
- Handle Supabase errors gracefully (toast/log + fallback behavior that DOES NOT use placeholder recipes).
- Avoid additional network calls where possible; use caching if it already exists.

OUTPUT REQUIREMENTS (Plan Mode)
You MUST produce:
1) Investigation checklist:
   - Where Auto-populate Week currently gets recipes from (exact file/function names).
   - Where “For You” gets recipes from (exact file/function names).
   - Where placeholder data still exists and whether it’s used in production flows.
   - Where E2E tests live + how they are run in this repo (paths + commands).
2) Proposed shared API:
   - Define a single function/hook like:
     getPersonalizedRecipes({ userId, context: "forYou" | "planner", constraints })
   - Explain inputs/outputs briefly.
3) Step-by-step tasks (small diffs):
   - Task 1: identify current placeholder path
   - Task 2: implement Supabase query for recipes with pagination/limit
   - Task 3: unify preference application
   - Task 4: wire Planner Auto-populate to shared logic
   - Task 5: remove/guard any remaining placeholder usage
   - Task 6: run E2E tests + address failures
4) Acceptance criteria checklist (must be very concrete; see below).

ACCEPTANCE CRITERIA (must be included verbatim and expanded with specifics)
- [ ] Auto-populate Week pulls recipes from Supabase in all runtime environments (dev/prod).
- [ ] No placeholder recipe arrays/JSON are referenced by Auto-populate Week code path.
- [ ] For You and Auto-populate Week call the same preference application function/service/hook.
- [ ] Preference filters/exclusions match between both features (verified by at least 3 test scenarios).
- [ ] Empty Supabase results show an empty state (no placeholder fallback).
- [ ] Supabase error shows a clear error state and does not crash.
- [ ] Existing E2E test suite runs successfully after changes (report command + summary).
- [ ] No unrelated refactors; only minimal necessary changes.

IMPLEMENTATION RULES (these are the only “always rules”)
- Make the smallest possible diff that satisfies the requirements.
- Do not refactor unrelated code.
- If you discover ambiguity (e.g., which preference fields are authoritative), STOP and ask rather than guessing.
- Add lightweight logging (dev-only) only if needed to prove source-of-data during verification.
- Create a Replit checkpoint after each completed task.
- Do not stop after implementation; E2E must be run and results reported.

DECISION-TIME CHECKLIST (put this at the END)
Before editing any file, confirm:
- Is this change explicitly required by the scope?
- What is the smallest diff?
- Which files will be touched, and why?
- How will we test this immediately (manual steps + E2E command)?