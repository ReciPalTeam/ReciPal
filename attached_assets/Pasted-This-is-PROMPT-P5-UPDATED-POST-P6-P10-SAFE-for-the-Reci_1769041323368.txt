This is PROMPT P5 (UPDATED, POST P6–P10 SAFE) for the ReciPal codebase.
You already completed P6–P10. This prompt must NOT break any of that work.

SCOPE (HARD-LOCK)
- Modify ONLY:
  1) Recipes feed card component(s) used by For You / Something New / Favorites
  2) Recipe Detail CTA area where “Get X” exists
  3) Cart add/dedupe utility functions (logic only; do NOT change Cart UI)
- Do NOT modify:
  - Planner (all logic/UI from P1–P9)
  - Profile/Dashboard (P8/P9)
  - RevenueCat/paywall/subscription logic
  - Pantry UI
  - Cart page layout (P10 collapse/reorder must remain exactly as-is)
  - Recipes filter chevrons UI (P10)
  - Side menu dark mode styling (P10)
  - Global wording (Maybe) (P10)
- Do NOT change the Recipes feed card layout except to ADD a second CTA button.
  - Do NOT move existing buttons, text, time/servings chips, nutrients, titles, etc.
  - Preserve spacing/typography exactly as currently styled.

GOALS (ALL REQUIRED)
A) Recipes feeds (For You / Something New / Favorites):
   - Add a second CTA button on each recipe card: “Add to Cart”
   - Must use CART icon
   - Must use GREEN 3D bubble gloss styling consistent with existing CTAs
   - Must NOT replace or modify the existing “Add to Plan” CTA (plus icon) or its behavior
   - Behavior: tapping “Add to Cart” on a feed card opens Recipe Detail for that recipe (it does NOT add ingredients directly from the feed).

B) Recipe Detail:
   - Replace the existing “Get X” CTA with a GREEN 3D bubble gloss “Add to Cart” button (cart icon).
   - Clicking “Add to Cart” opens a dedicated Add-to-Cart modal (NOT the Add-to-Plan scheduler).

C) Add-to-Cart Modal:
   - Title: “Add to Cart”
   - Servings stepper: 1–10 with 10 displayed as “10+”
   - Primary button: “Add to Cart”
   - Secondary button: “Done” (closes modal)
   - Must not affect planner meals; this is cart-only.
   - After adding, show toast: “Added to cart”
   - If already added recently with same servings, toast: “Already added”

D) Cart add behavior:
   - Adds all ingredients for the recipe (scaled by servings where structured quantity exists)
   - Dedupes against:
     1) existing Cart items
     2) ingredients already implied by meals currently in the Planner for the current week
   - Must avoid re-adding already covered items.
   - Must NOT change Cart UI (P10’s reorder/collapse stays untouched).

IMPORTANT INTENT
- This supports the “I don’t use the planner” customer path:
  browse recipes → open recipe detail → add meals to cart → checkout.

────────────────────────────────────────
1) RECIPES FEED CARD: ADD SECOND CTA (SAFE UI ADD)
────────────────────────────────────────

1.1 Add a new CTA button to the recipe card component used in the Recipes feed toggles:
- Label: “Add to Cart”
- Icon: cart icon
- Style: GREEN 3D bubble gloss (must match existing gloss/3D CTA style)
- Placement: directly below or adjacent to existing “Add to Plan” CTA
- DO NOT move/replace/remove:
  - existing CTA(s)
  - recipe title
  - nutrient/time/servings UI
  - any other content

1.2 Behavior:
- Clicking “Add to Cart” on a feed card should navigate to/open Recipe Detail for that recipe.
- It MUST NOT add ingredients from the feed directly.
- The user adds ingredients only from within Recipe Detail.

────────────────────────────────────────
2) RECIPE DETAIL: REPLACE “GET X” WITH “ADD TO CART”
────────────────────────────────────────

2.1 Find the existing “Get X” CTA on Recipe Detail and replace it with:
- “Add to Cart” (cart icon)
- GREEN 3D bubble gloss styling

2.2 “Add to Plan” on Recipe Detail remains unchanged.

2.3 Clicking “Add to Cart” opens the Add-to-Cart modal below.

────────────────────────────────────────
3) ADD-TO-CART MODAL (SERVINGS + DONE)
────────────────────────────────────────

3.1 Modal UI:
- Title: “Add to Cart”
- Subtext: “Choose servings to scale ingredients.”
- Servings stepper:
  - values 1..10
  - display “10+” at 10
- Primary CTA: “Add to Cart”
- Secondary CTA: “Done” (closes modal)

3.2 Behavior:
- Add to Cart does not close modal automatically unless that is already app pattern; acceptable either way.
- Done always closes modal.
- Show toast on add success.

────────────────────────────────────────
4) CART ADD + DEDUPE LOGIC (ROBUST, NON-DESTRUCTIVE)
────────────────────────────────────────

4.1 Coverage Sources for dedupe:
A) Existing cart items:
- Use existing cart state/store.
B) Planner implied ingredients:
- Derive ingredient coverage from all planned meals in the current week (regardless of cooked status).
- Include swapped ingredient overrides if present.
- Normalize names.

4.2 Normalization function (must exist and be reused):
normalizeIngredientName(name) that:
- lowercases
- trims whitespace
- strips common descriptors (fresh, organic, raw)
- removes punctuation
- optionally strips numeric quantities if embedded in name

4.3 Deduping rules:
- If normalized ingredient is already in cart, do not add again.
- If normalized ingredient exists in planner implied set, do not add again.
- This is “avoid re-adding” behavior as requested.

4.4 Quantity behavior (minimal safe approach):
- Do NOT inflate quantities aggressively in this prompt.
- Prefer “first add wins” and avoid duplicates.
- Store servingsUsed metadata for the cart entry so later we can improve quantity aggregation.

4.5 Anti-spam:
- If the user tries to add the same recipe to cart again with same servings within a short time window, do not duplicate; show “Already added”.

4.6 Data model for cart items from recipe:
Store:
- id
- ingredientDisplayName
- ingredientNormalizedName
- quantity (if structured)
- unit (if structured)
- sourceRecipeId
- servingsUsed
- createdAt

────────────────────────────────────────
5) VALIDATION CHECKLIST (MUST PASS)
────────────────────────────────────────

1) Recipes feed cards (For You / Something New / Favorites) now show BOTH CTAs:
   - Add to Plan (unchanged, plus icon)
   - Add to Cart (new, cart icon, green 3D bubble gloss)
2) Feed card layout is otherwise unchanged (no regressions to spacing, buttons, nutrient/time/servings/title).
3) Feed-card Add to Cart opens Recipe Detail and does NOT add ingredients directly.
4) Recipe Detail “Get X” is replaced by “Add to Cart” (green 3D bubble gloss, cart icon).
5) Add-to-Cart modal exists with servings stepper + Add to Cart + Done.
6) Adding to cart dedupes against:
   - current cart items
   - planner-implied ingredients (current week)
7) Cart UI (P10 reorder/collapse/expand overlay) remains exactly as currently implemented.
8) No regressions to Planner, Profile/Dashboard, RevenueCat, Pantry, Recipes filters, or side menu.

Do not mark complete unless all 8 validations pass.
